node_modules/
test-results/
playwright-report/
.playwright/
.env

# Full OpenCASCADE WASM (~65MB) — opt-in BREP topoloji motoru.
# "npm run occt:full" ile node_modules'tan kopyalanir; git'e commit EDILMEZ.
# Deploy'da self-host asset olarak ayrica sunulmali (bkz. vendor/opencascade-full/NOTICE.md).
vendor/opencascade-full/*.wasm
vendor/opencascade-full/*.js
