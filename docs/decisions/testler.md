# Test dosyaları — hangi test neyi kolluyor

> Kök `CLAUDE.md`'den taşındı. Metin birebir korunmuştur.

## Test Dosyaları

| Dosya | Test Edilen Modül | Kapsam |
|-------|-------------------|--------|
| `tests/unit/signal-tree.test.js` | `js/signal-tree.js` | Arama katlaması, üç-durumlu grup, istatistik, mini eğri, kanal toplama |
| `tests/unit/trace-view.test.js` | `js/trace-view.js` | Şerit uzlaştırma, ayrık/metin kanal tespiti, Y aralığı, şerit yerleşimi, tutamak isabeti, logaritmik X ve Y ekseni, **birleşik şeritte sinyal başına Y ekseni**, açılır pencere kapatıcısının GECİKMESİZ bağlanması |
| `tests/unit/measure-core.test.js` | `js/measure-core.js` | Örnek kilitleme, pencerenin tek X ekseni kuralı |
| `tests/unit/numerics.test.js` | `js/numerics.js` | PCHIP spline, RK45 solver, enerji dengesi |
| `tests/unit/mount-signals.test.js` | `js/mount-signals.js` | Takoz kanalları: FRF ızgarası, Campbell mertebe/mod çizgileri, F(δ) yasası, ivme süpürmesi, kanal kimliği, diyagram yorumu |
| `tests/unit/mount-brief.test.js` | `js/mount-brief.js` | Yorumun sayısal çekirdeği: tepe bulma, izolasyon başlangıcı, ilk çekme, ara değerleme, Campbell kesişim devri |
| `tests/unit/mount-capacity.test.js` | `js/cp-mount.js` + `js/mount-brief.js` + `js/cp-mount-report.js` | Taşıma kapasitesi: kg→N dönüşümü, kapasite çizgisi, % kullanım, Rapor sütunu |
| `tests/unit/mount-isolation-3dir.test.js` | `js/cp-mount-report.js` | Ateşleme frekansında X/Y/Z izolasyon yüzdesi (tam 6 SD), tedarikçi raporuyla karşılaştırma |
| `tests/unit/mount-shock.test.js` | `js/mount-core.js` şok + `js/mount-signals.js` | Newmark-β geçici rejim: yarı-statik limit, sönümsüz enerji korunumu, modal salınım frekansı, **metal-metal durdurucunun gerçekten sınırlaması** (daha az yol, daha çok kuvvet) |
| `tests/unit/mount-nonlinear-transient.test.js` | `js/mount-core.js` `shockResponse` + `buildKfromBasis` + FRF | **İki sessiz hatanın kapısı**: (1) şok çözümü lineerdi — statik taraf Newton koşarken geçici rejim sabit K ile tek çözüm yapıyordu; (2) rijitlik tabanı üç ayrı yerden geliyordu (modal tanjanttan, FRF/şok nominal k_dyn'den) → aynı panoda modal frekans ile FRF tepesi ayrışıyordu. Kilitler: yarı-statik limitte Newton'un `solveCaseNL`'e inmesi + kalan farkın sönümle küçülmesi (atalet artığı, model sapması değil), küçük genlikte şok salınımının TANJANT moda oturması, lineer modelde Newton yolunun eski çözümle birebir eşleşmesi, ve TTAR referans modelinde durdurucu payının statik önyükle birlikte ölçülmesi |
| `tests/unit/arac-example.test.js` | `js/cp-arac-example.js` + `assets/examples/ap_*` | Araç Performans örnek kartları: kayıt defteri ↔ disk tutarlılığı, öksüz dosya yok, her örnekte çalışır güç aktarma zinciri |
| `tests/unit/node-label-anchor.test.js` | `js/components.js` `veNodeLabelAnchor` + `js/export-topology.js` + `js/topology.js` | Düğüm ADININ çizim çıpası tek kaynaktan: `node.data.labelPos` (üst/sol/sağ/alt) yalnız canlı kanvasta okunuyordu; dışa aktarılan SVG/PNG ve şerit paneli adı hep kutunun ALTINA çiziyordu — ekranda tellerden kaçırılan ad çıktıda yine şeritteydi |
| `tests/unit/arac-example-layout.test.js` | `assets/examples/ap_*` + `js/cp-arac-performans.js` | **Kanonik yerleşim kapısı**: şaft ekseni (zincirin dört bağlantısı tam yatay), fan simetrisi, dik açılı telin ne bileşenin ne de bir ADIN üstünden geçmesi, kutu çakışmaması, `lineType` sözleşmesi (zincir `curve` / dal `stepped`), bütün örneklerin AYNI sütunlarda olması, `VE_ARAC_PERFORMANS_LAYOUT` ile birebir uyum (6×6'da fan dışı + fan sütunları), "Örneği Aktar"ın eklediği `ap-example` düğümünün boş köşeye düşmesi; **o düğümün yeri ÖRNEĞİN KENDİ motorundan** (`veApExampleSlot`) ve kutusu da adı da hiçbir düğümün kutusuna ve ADINA binmiyor (kamera tabanlı yerde 15 örneğin 6'sında motorun uzun adının içine düşüyordu) |
| `tests/unit/arac-performans-shift-4500sp.test.js` | `js/cp-gearbox.js` | Allison 4500 SP S1–S4 vites eşikleri; 2C→2L avlanma bekçisi + bütün profillerde yapısal kapı |
| `tests/unit/matching-selection.test.js` | `js/cp-gearbox.js` + `js/cp-torque-converter.js` + `assets/examples/ap_*` | "Hangi şanzıman/konvertör seçili?" çözümü (anahtar → ad → oran/eğri eşleşmesi) + örneklerin preset anahtarı sayılarla tutarlı |
| `tests/unit/arac-example-data.test.js` | `assets/examples/ap_*` | **Girdi bütünlüğü**: kütle/alan/Cd/aks/Crr/vites/transfer/governed/aksesuar/pumpTorqueDrop iSCAAN raporlarıyla doğrulanmış değerlere sabitlendi |
| `tests/unit/arac-example-calibration.test.js` | `js/ft-performance.js` + `assets/examples/ap_*` | **Filo kalibrasyon kapısı**: 13 örnek gerçek zincirle koşuyor — stall devri, tavan hız (iSCAAN Performance Summary bandı), 0-20/40/60/80 altın çıpaları, **81 vites geçiş noktası** + avlanma bekçisi |
| `tests/unit/arac-performans-shift-2957sp.test.js` | `js/cp-gearbox.js` | Allison 2957 SP Wide DynActive: dokuz geçişin de N_motor 2400'e oturması, a = 1/i kuralı |
| `tests/unit/arac-performans-transfer-roles.test.js` | `js/ft-performance.js` | Transfer kademe rolü orandan çözülüyor (dizi konumundan değil) |
| `tests/unit/gear-efficiency.test.js` | `js/ft-performance.js` + `js/cp-gearbox.js` | Dişli verimi: şanzıman bazlı ölçülmüş katsayılar, vites başına tablo (2957 SP), stall kaybı, geri viteste NaN yok |
| `tests/unit/mount-adams-tulga.test.js` + `tests/fixtures/adams-tulga.js` | `assets/examples/tulga_topoloji.json` ↔ Adams referansı | **Karşılaştırmanın defteri** (doğruluk kapısı değil): Adams'ın Tulga tablosu ve Adams'ın koştuğu GİRDİ dondurulmuş. Üç şeyi sayıyla tutar: (1) statik taraf örtüşüyor — 10 temiz yük durumunda RMS 0,067 mm, tablonun 0,1 mm yuvarlamasının altında, ve iki varyantta AYNI (k_dyn statik çözüme girmez); (2) iki açık model farkı — Adams'ta olup MFSim'de olmayan yanal ~12 mm durdurucu ve ters tork yalpa işareti; (3) kapanmamış modal sorusu — Adams, TK040 dinamiği TAKASLI girdiye (RMS 0,539 Hz) kataloglu girdiden (1,010 Hz) daha yakın, yani Adams'ın girdisi de muhtemelen takaslı. MLMT-0216-33-TK040 raporu depoda YOK; geldiğinde hangi tarafın oynayacağı bu sayılardan okunur |
| `tests/unit/mount-example-library-match.test.js` | `assets/examples/*_topoloji.json` + `js/mount-core.js` `MOUNT_EXAMPLES` + `js/cp-mount.js` `VE_MOUNT_LIBRARY` | **Örnek modeller katalogla tutarlı mı**: Tulga'nın iki TK040 takozunda dinamik rijitliğin X ile Z'si takas girilmişti (katalog 740/355/335, örnekte 335/355/740) ve İKİ kopyada birden duruyordu. Sessizdi çünkü statik çözüm yalnız `k_stat` kullanır — 210 çökme değerinin hiçbiri değişmiyordu; hata yalnız modal/FRF/sönüm/şok tarafında görünür. İki kapı: statik üçlü kütüphanede tek bir kayıtla eşleşiyorsa dinamik üçlü de o kaydınki olmak zorunda; ve dinamik/statik oranının eksene göre yayılımı ≤ 1,5 (27 katalog girdisinin en kötüsü 1,117, takaslı hâl 4,701 — eşik ikisinin arasında) |
| `tests/unit/mount-example-names.test.js` | `js/mount-core.js` örnekleri + `assets/examples/*.json` | Örnek modellerde ad ↔ konum tutarlılığı (Sağ/Sol ↔ Y işareti), tekrarlı ad, iki kopyanın ayrışmaması |
| `tests/unit/mount-results-tab.test.js` | `js/results.js` + `js/graphics.js` | Takoz çözüm sekmesi, tek X ekseni kuralı, pano uzlaştırma |
| `tests/unit/mount-results-publish.test.js` | `js/cp-mount.js` | Çözümün panoya yayını; alt-topoloji çökertme regresyonu |
| `tests/unit/fead-layout-plane.test.js` | `js/fead-model.js` `VE_FEAD_VIEW_FRONT` + `js/cp-fead.js` `veFeadLayoutSVG` + `veFeadMmToCanvas` | **ÇİZİLEN RESİM RAPORUN RESMİ Mİ**: on Gates raporu için, üretilen SVG'nin `circle[data-pi]` merkezleri raporun kendi "Layout Data" koordinatlarıyla ÇİFT ÇİFT karşılaştırılıyor (sol-sağ + alt-üst; sıralama DEĞİL çift, çünkü iki kasnak aynı X'i paylaşabiliyor ve tie-break keyfi olurdu), gergi ile krankın yanı ayrıca — kullanıcının bildirdiği belirti buydu; **kanvas ↔ kart bağı** (kanvas X'i hiç aynalanmıyor, yani ayna açıkken aynı modelin iki resmi tersti); ayna makinesinin tam simetrisi ve `rawFrame`'in varsayılanla aynı resmi vermesi; **oniki örneğin onikisinde de KRANK CW** (liste `sense` +1, `spin` −1, etiket `↻ CW`) ve ters rotanın bedeli ters işaretle (ileri −1 / geri +1) |
| `tests/unit/fead-core.test.js` | `js/fead-core.js` + `tests/fixtures/fead-validation.js` | **FEAD çekirdeğinin doğrulama kapısı**: 17 Gates raporu / 2095 değer (çalışma %0.5, Load dahil %1.5, kol açısı 0.2°), 8 koruma mekanizması, SPEC §9 yapısal özdeşlikleri (sarım değişmezi, `L_pitch−L_eff=2π·hb`, çevrim kapanışı, sürücü gücü), UMD tarayıcı köprüsü; **burulma modeli**: yapısal özdeşlikler (tam 1 rijit cisim modu, take-up özdeşliği %0.01, yalnız gergi komşusu spanlar) + Gates "System Resonance (Mode 1)" kalibrasyonu (5 sistem RMS <%8, 4/6/8PK kaburga ölçeklemesi) |
| `tests/unit/gates-archive.test.js` | `docs/gates-reports/pdf/` + `tests/helpers/gates-pdf.js` + `tests/fixtures/fead-validation.js` | **Arşiv KAPISI — testler Gates PDF'lerini doğrudan okuyor.** Okuyucu saf Node + `zlib` (yeni bağımlılık yok) ve üç sessiz kusuru çözmek zorunda: font BAŞINA ToUnicode (birleştirme dört raporda çöp metin üretiyor — glif 44 → `space`/`A`/`#`/`@`/`G`), eksi işaretinin AYRI çizim çağrısı olması (`["-","72.00"]` → kaçırılırsa kasnak aynalanır), nesne akışları (ObjStm — AG00894'te tek font bile bulunamıyor). Fixture'ın **284** statik değeri (yerleşim · çap · açıklık · sarım · gergi künyesi) on raporun tamamında **0 uyuşmazlıkla** geri üretiliyor. Belge bütünlüğü: `Page N of M` ile alıntı tespiti — üç rapor alıntı, yedisi tam (AG00879'un sayfa AĞACI 5 gösteriyor ama 12 sayfa da içinde). Altbilgi tek satır ve birleşik (`Page 1 of 119.37.0.0`) → düz kalıp **119** okur  Ayrıca **gergi künye kütüphanesini** (`js/fead-tensioners.js`) kaynağına bağlar: on kaydın kol/oran/moment değeri (30 sayı) birebir, ön yükün dokuzu birebir + onuncusu `preloadDerived` ile işaretli (raporda YOK), kaburga sayısı raporun künyesiyle aynı (AG00810 bir dönem 8 yazıyordu, raporu 10PK), parça kodu Drive Notes'ta gerçekten geçiyor ve doğrulanamayan dört kayıt kod TAŞIMIYOR |
| `tests/unit/fead-canvas-mm.test.js` | `js/fead-model.js` mm katmanı + `js/connections.js` imza | **Kanvas ↔ mm köprüsü 2026-09-09'da kutularla birlikte KALKTI**; kalan üç kapı mm tarafının hâlâ canlı olan parçaları: orijin KİM (sürücü kasnak, rol), gergi kutu noktasının TEK okuyucusu (`veFeadTensionerBoxMm` — Kayış Tablosu gergi satırını oradan alıyor; ikinci bir kural satırı boş bırakırdı), ve **orijin göçünün BEDAVA olması** (öteleme geometriyi 0.00e+0 değiştiriyor). Ayrıca topoloji imzasına kasnak konumu girer, araç düğümü GİRMEZ |
| `tests/unit/fead-duty.test.js` | `js/fead-duty.js` + `js/cp-fead.js` + `index.html` | **Çalışma çevrimi kütüphanesi**: yedi kaydın yedisi de kaynağında (fixture + BMC örneği) BİREBİR var — ikinci kopya sessizce ayrışırsa kullanıcı çevrim seçer, model çözülür, uyarı çıkmaz, yalnız ömür ve yorulma payları raporundan başka çıkar; arşivin TEK çevrim göstermediği (≥6 desen) — kütüphanenin var olma sebebi; Σ%zaman = 100, devir dizisi artan, anahtarlar tekil, künyede kaynak YAZILI; liste KOPYA döner (katalog güncellemesi eski projeyi bozmaz), satırlar kW'ı BOŞ üretir (güç hesaplanır), bilinmeyen anahtar uydurma kayıt üretmez; eşleme gidiş-dönüş tutar, tek devir değişince NULL döner ve kW'a BAKMAZ; etiket tek üreticiden ve iki yüzey de onu çağırıyor; betik `index.html`'de cp-fead ve sihirbazdan ÖNCE (çıpa script etiketi — çıplak dosya adı bir YORUMU bulup sırayı yanlış okuyor), tohum panel KURULURKEN atılıyor; her kayıt KAYNAĞININ sıcaklığını taşır (70 · 80 · 90 °C; paylaşılan çevrimde çoğunluk) |
| `tests/e2e/fead-duty.spec.js` | Çalışma çevrimi (uçtan uca) | **GERÇEK tarayıcı**: sihirbaz taze açılışta dolu tabloyla geliyor ve çevrim seçicisi yerinde, açılır pencereden GERÇEK seçim (`selectOption`) tabloyu o kayıtla değiştiriyor (700·1200·2000·3000), kW hücreleri kaynağıyla birlikte basılıyor; **panel** de `showNodeProperties` ile 6 satır ve seçiciyle kuruluyor ve *"Henüz devir noktası yok."* mesajı YOK. Panel yolu Node'da HİÇ koşmuyor |
| `tests/unit/fead-belts.test.js` | `js/fead-belts.js` + `js/fead-model.js` aday değerlendirmesi | **Kayış kataloğu**: listelerin sıralı/tekil/pozitif olması, aralıkların ContiTech beyanıyla çakışması (bir listenin yanlış profile yapışması ancak böyle yakalanır), en kısa boyun min. kasnak çevresinden büyük olması, `veFeadBeltStock`'un KOPYA döndürmesi. **Izgara bir kural**: en yakın adıma yuvarlama, aralık dışında kenetlenme, ızgarası olmayan profilde sessizce PK ızgarasının kullanılmaması. **Kod**: otomotiv ve endüstriyel yazımın ikisinin de çözülmesi, gidiş-dönüş, kaburga denetiminin yalnız verisi olan profilde hüküm vermesi. **Ölçülmüş boşluk**: 8PK 1715'in endüstriyel listede OLMAMASI (komşuları 65 mm uzakta) — kataloğun iki kümeli olmasının sebebi. **Uçtan uca**: serbest kipin gereken boyu → katalog ızgarası → sabit kip tabanı (kol 28.4271° · T 532.142 · hub 302.125) birebir; sığmayan adayın gerginlik YAZMAMASI (4.05e10 N sızmıyor), boy uzadıkça kol ve gerginliğin düşmesi, aday değerlendirmesinin çalışma noktası önbelleğini kirletmemesi |
| `tests/unit/fead-belt-mode.test.js` | `js/fead-model.js` kayış kipi + `js/fead-core.js` hoşgörülü geometri | **Kayış boyu sabit değil**: kip çözümü ve geriye dönük uyumluluk (boyu olan eski proje `fixed`, boyu olmayan artık ÇÖZÜLÜYOR); sabit kipte tabanın BİREBİR korunması (kol 28.4271° · L 1715.0000 · T 532.142 · hub 302.125); **nominal kol açısı yay künyesinden, geometriden DEĞİL** — montaj merkezi ya da pivot yokken de türetilir, künye gerçekten eksikse NaN kalır (uydurulmaz) ve `direct` kol açısı kipinde serbest kayış NOMİNALE oturur, aralığın ortasına DEĞİL (eskiden kol 38.1174° · fallback true · uyarı 0); serbest kipte gerginliğin ankraj, boyun ÇIKTI olması ve iki kipin doğru modelde AYNI çalışma noktasına varması; sürüklerken çözümün kopmaması (−200…+40 mm, boy monoton). **Kenetleme**: kuşatılmış hedefte çekirdeğin çözümünün birebir dönmesi, erişilemeyen hedefte istisna yerine sınır + aralığın yazılması, sığmayan kayışta NOMİNAL kol açısına düşülüp ÖNERİLEN boyun serbest kipinkiyle aynı çıkması, kenetlenmişken uyuşmazlık uyarısının İKİNCİ KEZ basılmaması. **Hoşgörülü geometri**: kapanmayan çevrimin çözülüp `geomValid:false` ile yazılması, çekirdek varsayılanının hâlâ ATMASI, çakışan kasnakların tek gerçek durdurucu olması. **Üç sessiz hata**: `feasibleRelMax` ölçütü, `_geomOpt`'un sistem ömrünün başında kurulması, dejenereliğin SARIM değil TAKE-UP ile ölçülmesi |
| `tests/unit/fead-model.test.js` | `js/fead-model.js` | **Köprü kapısı**: AG00686 MFSim KANVAS DÜĞÜMÜ olarak kurulup Gates sayılarını üretiyor mu (span %0.5, sarım 0.2°, Mean kol açısı 0.2°); temas tarafı üç katmanlı çözümü, sürücü rolü, `dia→od` göçü, ad tekilleştirme, hata çevirisi. **Güzergâh teşhisi**: tel silinince çözüm ARTIK aynı kalmıyor (eskiden kopuk kasnak sıraya sessizce ekleniyordu), kopuk kasnak adıyla bildiriliyor, kapanmayan zincir ve çatal (bir kasnaktan iki tel) sebebiyle yazılıyor, `veFeadRouteOrder` sözleşmesi (yerleştirici için bütün kasnaklar) korunuyor. Ayrıca **ters temas tarafının hata VERMEDİĞİNİ** belgeler (rozetin varlık nedeni). **Duty kapısı**: AG00686 duty tablosunun çıkış gerilmeleri ve hubload'ları %0.5 içinde; kW'ın kimlikle anahtarlanması (yeniden adlandırmada kaybolmuyor), sürücü gücünün toplamdan hesaplanması, ateşleme frekansı, yorulma dağılımının çapa bağlı olması, katalog oranının ÇAPTAN hesaplanması. **Sıcaklık kapısı**: satır başına °C → tek °C indirgemesi hasar-eşdeğer (tek sıcaklıkta birebir aynı, dağılımda aritmetik ortalamanın üstünde), ağırlık `dc·v`, açıkça girilen %0 sıfır ağırlıklı; **yorulma modeli** seçimi dağılıma geçer, mutlak ömre geçemez ve bu yazılır. **Burulma köprüsü**: gergi kasnak kütlesi ve KRANK MİLİ ataleti çekirdeğe geçiyor (ölü girdiydi), krank adla anahtarlanır, `analyze()` içindeki çift hesap kapalı, eksik atalet sessiz değil; **gergi krankın ÇIKIŞINDA mı** — hüküm KAYIŞ sırasında (`tensionerOrder.index` kayış sırası, `afterDriver`; `last` çekirdek karşılığı): krankın girişindeyse uyarır ama durdurmaz, çıkışındaysa hüküm yine yazılır; çekirdek sırası kablo sırasının çevrilmişi (krank sabit, kalanı ters) |
| `tests/unit/fead-spin.test.js` | `js/fead-model.js` yön + gergi tarafı · `js/cp-fead.js` rozet/panel/teşhis | **Dönüş Yönü**: yön rota sırasının dolanım işaretinden gelir (çekirdeğin `loopSense` ölçütü, ikinci kopya yok), ters kablolama işareti çevirir, gergide MONTAJ merkezi kullanılır ve koordinat eksikse yön 0 (uydurulmaz); **rota kablolardan çevrilir** — uçlar YERİNDE takas edilir (yeni kimlik yok), iki kez çevirmek birim işlem, yalnız iki ucu da kasnak olan teller çevrilir; **geometri BİREBİR** (kasnak başına sarım, L_eff, Σ=360, kol açısı — hepsi 9 basamak) ama **gerilme zinciri değişir** (AG00976 ileri min 544,0 N ↔ ters min −291,6 N); **gergi tarafı hükmü** eşiksiz (ankrajın altına inen span aranır, negatif sayı değil), ileri yönde geçer / ters yönde sebebi adıyla yazar ve ULAŞILAMAZ çareyi (*"tasarım gerginliğini yükseltin"*) YASAKLAR, uyarı üst seviyeye yükselir (raporlar yalnız oraya bakıyor), çöken zincirde kayma hükmü verilmez; **rozet** glifle durumu (`↺ CCW`/`↻ CW`) renkle hükmü taşır ve CW/CCW AYNI renktedir, yön ROTA sırasından okunur (düğüm dizisinden değil), çözümden SONRA tazelenir (bir çözüm geride kalmaz); sözleşme (0/0, `maxInstances:1`, palet + kayıt defteri + `cp-core` dağıtımı) ve **silme kancası YOK** — durum kablolarda; **zarf kipinde merkez TÜRER** — `cenX/cenY` orada hiç yazılmadığı için yön okunamıyordu (sense 1 yerine 0, rozet `—`), artık `veFeadTensionerCenter` pivot + kol boyu + seçilen kol açısından çözüyor ve sonuç çekirdeğin çalışma merkeziyle birebir; `build.spin` zarf seçiminden SONRA tazeleniyor (bir çözüm geride kalmıyor); bayat `cenX/cenY` zarf kipinde okunmaz, mount kipinde türetmeye düşülmez; pivotu çokgene koymak sentetik düzende işareti ÇEVİRİYOR (iki gerçek örnekte çevirmiyor — kapı bu yüzden sentetik); **LİSTE SIRASI KAYIŞIN GİDİŞİNİN TERSİ (2026-09-08)** — `spin = −loopSense(liste)`, krank CW: çekirdek zinciri liste sırasında sürücüde +P/v (liste çıkışı gergin → liste gidişin tersi), AG00976 PDF'inin kendi okları (`A_C>FAN · ALT>A_C · FAN>ALT`, üçü de tablonun tersi) ve gerilme satırı (`1585 1582 1177 1174 546 544`: aksesuarda düşüyor) PDF'TEN OKUNUYOR, ekrandaki sürücü oku oniki örnekte de sweep=1 (CW) ve sırttan temas edenler 0, yükteki `spin` rozetle aynı işaret / `sense` tersi; **KABLOLAR GİDİŞ SIRASINDA (2026-09-08, ikinci tur)** — `veFeadRouteFlip` involüsyon ve bütün uçları takas etmekle aynı sıra, oniki örnekte tel çokgeninin dolanımı = `spin` ve `build.order` = Gates tablo sırası (`ex.route`), kablo sırasını çevirmeden çekirdeğe vermek işareti ters çevirir (kapı) |
| `tests/unit/fead-wire-order-migration.test.js` | `js/state.js` `veApplyLegacyMigrations` + `js/fead-model.js` `veFeadMigrateWireOrder` / `veFeadMigrateBeltOrder` | **İKİ GÖÇ, SIRASI ÖNEMLİ.** Şema 2 → 3 (teller gidiş sırasına): teller bir kez çevrilir ve damga 3'e yükselir, gömülü alt topoloji (fead-analysis → subTopology) de geçer ve damgalanır (editör açılınca ikinci kez çevrilmez), sürüm 2'de düğüm verisi migrasyonu ÇALIŞMAZ (Cd 0.75 korunur — kademeli kapı), sürümsüz dosyada ikisi de çalışır, kayış olmayan teller dokunulmaz; göç eden eski AG00976 çözülür, gergi gevşek tarafta, krank CW, kayış boyu ve gerginlik bugünkü örnekle 9 basamak aynı; göç ATLANIRSA gergi gergin tarafa düşer (ölçülmüş gerekçe). **Şema 3 → 4 (2026-09-09, kablo kalktı):** sıra tellerden okunup `beltIndex`e yazılır ve kasnak-kasnak telleri SİLİNİR; sürüm 2'den gelen dosya önce telini çevirir SONRA indisini kurar (ters sırada her eski dosya ters numaralanırdı), kayış olmayan teller silinmez, sürüm 4 dokunulmaz. Göç kapısı düğüm dizisi BİLEREK ters kurulmuş bir kayıtla ölçülüyor: göç atlanınca sıra dizi sırasına düşüyor (`FAN,TEN,ALT,IDR2,A_C,IDR1`) ve gergi gergin tarafa geçiyor. **Şema 4 → 5**: eski kayda İKİNCİ KANVAS eklenir (şemanın sağına, `customName` ile, AYNI tipten) ve ikinci geçişte kurulmaz; eski tipli (`fead-run`) bir düğüm varsa ikinci kart EKLENMEZ. **Şema 5 → 6 (2026-09-11, tip teke indi)**: kayıtlı `fead-run` düğümü `fead-layout` + `katOn:'isletme'`e çevrilir ve GÖRÜNÜM birebir korunur (devir · titreşim · kullanıcı adı ve ön ayarı EZİLMEZ); 4 damgalı bir dosyada iki adım ÇAKIŞMAZ — tek işletme kanvası çıkar |
| `tests/unit/cp-fead.test.js` | `js/cp-fead.js` + `js/components.js` | FEAD sunum katmanı: **yön gülü** (kenetleme, kesir olarak saklama, hareketsiz tıkın hiçbir şey yazmaması, kancanın yalnız kanvas/panelde kurulması); **şerit ÖLÇÜLMÜŞ bir çarpışmaya bağlı** — gül çizime çarpmıyorsa ayrılmaz (varsayılan kartta ayrılmıyor, taşımak hiçbir şey kazandırmıyor), çarpıyorsa AYRILIR ve dar kartta eski davranış birebir korunuyor; **gül etiket engelidir ve bu KOŞULSUZ** (taşınmışken de — engeli `!moved`e bağlamak, gül şemanın ortasına sürüklendiğinde korumayı kapatıyordu; ölçüldü: 256 konumda çakışma 3 ↔ 61), ölçüt çapa noktası değil etiket KUTUSU (çapa ölçütü mutasyondan GEÇİYORDU); **türetilen boyun KÖKENİ** — sağlıklı modelde metin değişmiyor (yanlış alarm yok), künye eksikse *"tedarikçiye verilecek boy DEĞİLDİR"* diyor, kol kenetlendiyse *"nominal açısına oturamadı"* diyor, ikisinde de sayı kırmızı + `?`, ve kenetlenmenin SEBEBİ Kayış Özellikleri panelinde basılıyor (`veFeadWarningBox` orada YOKTU); **kart ölçüsü** — aşılmış her varsayılan (60×56 ve 420×340) yükselir, listedeki hiçbir çift güncel ölçü olamaz, bilerek verilen ölçü korunur; **örnek KULLANIMA HAZIR** — "Başlangıç ve Örnekler" düğümü örnek kurulunca KALMAZ, "Rapor" düğümü kurulur ve sol şerit sırası Kayış Özellikleri → Çözücü → Rapor çıkar (sıra `veFeadExampleNodes`'un push sırasının gözlenebilir sonucu), kayıtlı BÜTÜN örneklerde; **kanvas rozeti NEGATİF**: kasnak rozet ALMAZ (kutusu yok — K/S dalı kutular kalkınca erişilemez koda dönmüştü), yalnız kayış ve dönüş yönü alır; **FEAD tiplerinin HİÇBİRİNDE port yok** ve bu yüzden modül panelinde "Bağlantı" satırı da yok; `feadContact` varsayılanları, panel smoke testleri, alt-sistem sözleşmesi, `fead-*` tip tanımları; panelin HANGİ ALANLARI sorduğu (montaj merkezi ↔ serbest açı), servis faktörü hükmü; **kayışın kaburgalı yüzü** (diş yönü + aynalanmış çevrim), **telin komşuya bakan kenarı** (oran kuralı, elle taşınan portun kazanması) ve **`veFeadArrangeByCoords`** (kanvas mesafesi = mm mesafesi, Y TERS, kümenin ortalanması, koordinatsız kasnağın gizlenmemesi, araçların kümenin dışında kalması, koordinata DOKUNMAMASI, `veTidyLayout`'un devretmesi, `silent` kipinde saveState/toast'ın çağrılmaması, konumun 1 mm'ye KUANTALANMAMASI); **örnek kurucusu koordinatı yalanlamıyor** — `veFeadLoadExample` gerçek `createNode` ile koşturulup her kasnağın kutu merkezinin mm koordinatına oturması ölçülüyor (eski ölçekli yerleşimde sapma 38.108 mm) ve ilk sürüklemenin koordinatı KAYDIRMADIĞI, kayıtlı BÜTÜN örneklerde; **gergi kutusu kip başına doğru noktada** — zarf kipinde PİVOT, mount kipinde montaj merkezi, ve "Otomatik Düzenle" ile alt topoloji açılışı (`veFeadSyncCanvasFromMm`) AYNI yere koyuyor (sync tek kutuyu bile oynatmıyor); girdisi eksik gergi yine gizlenmiyor, kümenin altına diziliyor |
| `tests/unit/cp-fead-report.test.js` | `js/cp-fead-report.js` + `tools/report-assets/fead-theory-source.html` | **Rapor içeriği**: Türkçe sayı biçimi (gerçek eksi, `—` ≠ 0), `wearPct` oran→yüzde çevrimi, sarım açılarının DERECE basılması, Σsarım=360 ve `L_pitch−L_eff=2πh_b` denetimlerinin belgede görünmesi, sürücü kW sütununun duty tablosunda OLMAMASI, çözülemeyen konumun `Err.` ile işaretlenmesi, `undefined`/`NaN`/`[object` sızmaması, "ortalama tork ≠ peak", sistem burulma modunun yokluğunun yazılması, uygunluk hükmünün servis faktörünü kullanması, şekil/tablo numaralarının boşluksuz ve her üretimde sıfırlanması, şablon tokenlarının tek kez geçmesi, içindekiler id'lerinin üreteçle aynı olması; **tasarım gerginliğinin kaynağı**: (8.x) denklem zincirinin ELLE ÇALIŞILABİLİR olması (çevrim çarpanı bir kez TERS yazılmıştı — basılan denklem 650 N yerine 2,13 N veriyordu), girdi ↔ türev envanteri, take-up'ın GİRDİ OLMADIĞI, tasarım gerginliğinin TÜRETİLDİĞİ (T = M/(dL/dθ) formülü ve sayısı belgede, "sorulmaz" yazılı, eski karşılaştırma tablosu YOK, eski kayıttaki designTensionN raporu etkilemiyor); **φ kuruluşu**: her satırın φ'sinin BASILAN iki θ'dan yeniden çıkması, Σd·φ=360, sarım ve φ İŞARET yaylarının örtük merkezinin kasnak merkezinde olması ve süpürmenin kısa yola normalize EDİLMEMESİ (198°'lik sarımda 162° çizerdi); **§8.9**: take-up'ın ANLIK türev olarak adlandırılması, ortalama eğimin ayrı basılması, monoton olmaması; **etiket yerleştirici**: çakışma, kilitli alan ve çember engeli; **teori**: (4.3) türetmesi, §5.1 ankraj paragrafı, §10 sembolleri, şablona gerçekten girmiş olması; §8.12 merkezkaç payı oranın içinde ("katılmamıştır" hükmü geri gelmez) ve sırt μ Dayco ölçümüyle |
| `tests/unit/fead-anim.test.js` | `js/cp-fead.js` animasyon + `js/fead-model.js` kinematik | **Kayış Yolu kartının animasyonu**: ω·r = v özdeşliği, ağır çekim katsayısının REFERANS devre bağlanması (seçili devre bağlansaydı seçici işlevsiz kalırdı — istenmeyen alternatif de koşturulup belgeleniyor), diş adımının çevreyi tam bölmesi, diş sayısının faz boyunca sabit kalması, bir adımlık fazın deseni birebir kendine getirmesi, dişlerin GİDİŞ yönünde ilerlemesi, kol açısal hızının `d·v/r` olması (sırttan temas edende ters) ve kol ucu çevresel hızının kayış hızına eşitliği (kasnakta kayma yok), animasyonun YALNIZ kanvas kartında olması, fazın düğüm kimliğinde durması (yeniden kurulumda kayış zıplamıyor), uzun duraklamada `dt` kırpması; **faz kayışın GERÇEK gidişinde** (yürüyüşe göre AZALIR — liste gidişin tersi; kırpılmış karede tam −mmS·MAX_DT, +mmS·MAX_DT değil), animatör durumu testler arasında sızdığı için `_feadForgetResults` + kartsız tick ile sıfırlanır (eski kapı iki fazı da 0'da karşılaştırıyordu — boştu), **yükten okunan adım çevreyi TAM böler** (bağımsız yuvarlama 174 adımı çevreden 0,0023 mm uzun yapıyordu; pencerenin içindeki fazlarda diş sayısı sabit), yükteki `spin = build.spin = −sense`; **STROBOSKOP KAPISI (2026-09-09)** — sihirbaz önizlemesinin BİREBİR çağrısıyla: 260 mm/s 60 Hz'de 0,45 diş/kare (30 Hz'de 0,90 = geriye okunur), animatör kare başına çeyrek dişe kırpıyor, kırpma yönü değiştirmiyor, uzun duraklamada da çeyrek diş (`MAX_DT` tek başına 2,7 diş geçiriyordu) ve GERÇEK devirdeki kanvas kartı (0,098 diş/kare) kırpılmıyor |
| `tests/unit/fead-example.test.js` | `js/fead-model.js` örnekleri + FEAD_INFORMATION | **Tedarikçi sayfası çıpası** (Gates'ten bağımsız ikinci doğrulama): kayış boyu 1715 mm, kol boyu 90.0 mm, Spring Mean Load 22.07 Nm, tahrik oranı 1.1; sayfanın devir→kW tabloları; **sessiz kanalın** ölçülmüş belgesi (montaj merkezi ↔ serbest açı 2.6×); **tasarım gerginliği TÜRETİLİR**: örnek onu taşımıyor, T = M/(dL/dθ) kuruluşu, eski kayıttaki değerin yok sayılması, türetilemezse sessiz kalmaması |
| `tests/unit/fead-example-ag00976.test.js` | `js/fead-model.js` örnek kayıt defteri + `js/cp-fead.js` örnek kartı | **Gates raporu çıpası** — tedarikçiden DÖNEN rapor (AG00976 · 8PK1715HD · Ten@-250/110 · Corrected-IDR1) örnek olarak kurulup UÇTAN UCA geri üretiliyor. Doğrulama harness'ı köprüyü ATLIYOR (`makeSystem`i doğrudan çağırıyor); burada zincirin tamamı koşuyor. Referans değerler fixture'dan okunuyor, İKİNCİ KOPYA YOK. Kapsam: 12 span+sarım (0.2 derece/mm), 6 konum × 7 sütun (kol 0.2°, çalışma gerginliği %0.5, Load %1.5), duty 12×6 gerilme+hubload (%0.5), sürücü gücünün TOPLAMDAN hesaplanması, dış çap → raporun Pitch/Effective sütunları, hız oranının PITCH çapından gelmesi (sayfanın elle yazdığı 162/57 = 2.842'yi AYIRT EDİYOR), **tasarım gerginliğinin ve serbest kol açısının TÜRETİLİP** raporun kendi satırlarına oturması, kol boyu çapraz kontrolünün ısırması; **efektif boy 1714.6** (raporun REBL sütunu — katalog adı 1715 kullanılırsa AÇIKÇA kırılıyor), tolerans/aşınma yoksa kol zarfının tek noktaya ÇÖKMESİ, `kwByKey` → düğüm kimliği çevirisi (kayarsa aksesuar SESSİZCE 0 kW ile koşar), B10'un aralık dışı olduğunu MODELİN KENDİSİNİN söylemesi, burulmanın kalibrasyon takımı DIŞINDAKİ bir sistemde %8 bandına düşmesi ve krank MİLİ ataleti geçilmezse %29 kayması; iki örneğin (giden sayfa ↔ dönen rapor) AYRI KALMASI ve pivot farkının gerginliği %19.5 kaydırması; panel kartında `undefined` basılmaması |
| `tests/unit/canvas-space.test.js` | `js/canvas-space.js` + `css/styles.css` | Sonsuz ızgara deseni, "ev" kamerası, topoloji ortalama; **sınır çerçevesi düğüm ADINI da sarar** — `veNodeLabelOverflow` dört kenar için taşma (yanda 7+genişlik, alt/üstte 4+yükseklik, karşılıklı taşma İKİ YANA EŞİT), modül kartının adının taşma EKLEMEMESİ, alt payın hiç küçülmemesi (çerçeve yalnız büyür), ölçüm işlevi geçilmezse davranışın BİREBİR eski hâli, ve boşluk sabitlerinin `css/styles.css`'teki margin'lerle aynı olması |
| `tests/unit/module-start-center.test.js` | `js/components.js` `veStartModule` | Karşılama kartından gelen modül bloğu görünümün TAM ortasına düşer (kabuk senkronu ölçümden önce) |
| `tests/unit/module-loader.test.js` | `js/module-loader.js` + `js/components.js` `veStartModule` | **Modül giriş ekranı ve doğrudan giriş**: karşılama kartına tıklamak modülün İÇİNE götürüyor (eskiden kanvasta kart bırakıp orada kalıyordu, içeri girmek için AYRICA çift tık gerekiyordu ve bunu söyleyen hiçbir şey yoktu). **En sessiz hata sınıfı GERİ DÜŞÜŞTE**: ekran gösterilemediğinde (kap yok · hareket kısıtlı · meşgul) işler YİNE DE koşmak zorunda — koşmazsa tıklama hiçbir şey yapmaz ve hiçbir yerde hata çıkmaz; patlayan bir adım da diziyi durdurmuyor. Ayrıca: kaplama ANINDA geliyor (koreografi PANELE ait — kaplama solarak gelseydi ilk karede boş tuval görünürdü), kademe sayısı adım listesinden yazılıyor (çentik dolguyla hizalansın), çubuk genişliği q'dan geliyor yuvarlanmış yüzdeden değil, mesaj biten adımın adında KALMIYOR (sıradakinin ayrıntısını yazıyor — indisten okunan hâli bir duruş boyu yanlış adımı gösteriyordu), CSS çıkış süresi JS'teki `VE_MODLOAD_CIKIS` ile AYNI (kısa olsa ekran solmayı bitirmeden kaybolur, uzun olsa saydam kaplama tıklamayı yutar), doğrudan giriş TİPTEN beyan ediliyor (`componentDefs.moduleEnter`) ve diğer iki modül beyan etmediği için onlarda eski yol birebir duruyor; giriş varken uçuş ve toast KURULMUYOR (ikisi de ekranın altında kalırdı: uçuş `--z-widget`, toast 5000, ekran 9001) |
| `tests/e2e/modul-giris.spec.js` | `js/module-loader.js` (gerçek tarayıcı) | **Node'da hiç koşmayan halka**: dizi gerçek `requestAnimationFrame` üstünde yürüyor (jsdom'da hiç ilerlemez, birim kapısı onu stub'lıyor) ve karşılama slaytının karesi `getComputedStyle` ile seçiliyor. Gerçek tıklama → ekran görünüyor (FEAD künyesi, 3 adım, `--mfsim-kademe`=3, devralınan fotoğraf, uçuş yok) → kapanıyor ve `veFeadStack` derinliği 1 oluyor, açılış yüzeyi kurulu ve sihirbaz açık; doğrudan girişi olmayan modülde ekran HİÇ açılmıyor ve kök topolojide kalınıyor |
| `tests/unit/port-geometry.test.js` | `js/components.js` port geometrisi + `js/connections.js` | Bağlantı ucu ile port dairesi aynı noktada — dört kenar, çok port, aynalama; **`veSyncPortDom`** — kenar sonradan değişince (AYNALAMA) dairenin teli takip etmesi, elle taşınan portun ezilmemesi, kenar değişmiyorsa DOM'a hiç yazılmaması; **negatif kapı: FEAD kasnakları portsuz** — iki kasnak arasına tel kurulsa bile ne amber kayış sınıfı ne gidiş oku çizilir (`veConnDirMark`'ın matematiği duruyor, bugün çağıranı yok) |
| `tests/unit/module-card.test.js` | `js/components.js` alt-sistem kartı + sidebar modül satırı | Modül kartı: içerik özeti (alt topolojiden), kart ölçüsünün tek kaynağı, eski 80×66 kaydın yükselmesi, **ad elemanının taşınması** (kopyalansaydı yeniden adlandırma sessizce eskirdi); palet sembolü `componentDefs`'ten (index.html'de ikinci kopya tutulmadığına dair kapı) |
| `tests/unit/example-topology-center.test.js` | `js/cp-mount.js` + `assets/examples/` | Örnek JSON'ları kanvas merkezine açar |
| `tests/unit/topology-wiring.test.js` | `js/ui-core.js` `veTryConnectPorts` + `js/connections.js` topoloji imzası | **Kanvasta tel çekmek**: geçersiz port çifti (çıkış→çıkış, giriş→giriş, kendine) bağlantı kurmaz ama SESSİZ de kalmaz; doğru çift her iki yönden de aynı bağlantıyı kurar; topoloji imzası tel/düğüm değişince değişir, düğüm SÜRÜKLENİNCE değişmez (kart 30 karede 0 kez yeniden kurulur) |
| `tests/unit/fead-table.test.js` | `js/cp-fead.js` Kayış Tablosu (Pafta) + `js/fead-model.js` sıra | **Kasnakların sayısal giriş yüzeyi — 2026-09-26'dan beri Kayış Yolu kartının PAFTASI**: sütun kimlikleri kullanıcının hesap sayfasına karşı ölçülü (efektif çap = OD+2·hb / OD+2·hr, Σişaretli sarım 360°, L = Σspan + Σ(sarım·r)); türetilen sütunlar ÇEKİRDEKTEN; girdi sütunları geometri çözülemese de dolu; virgüllü ondalık (`63,5` → 63.5); satır taşıma ve sürücü kilidi; Dönüş Yönü METİN DÜĞMESİ ve `contact` yazar; ekle/sil (gergi tekil, ekleme gerginin önüne, `saveState` mutasyondan önce); görünüm CSS'te, durum kuralları jetondan, ölçü sabitleri CSS'le birebir; tazeleme TEK KAPIDAN ve bir kare sonra — ODAK ve yazılmamış metin korunur; **açıklık ön ayarın alanı** (`tablo`; düğme ön ayara dönünce alanı SİLER, ön ayar uygulamak temizler); **kart ölçüsü tabloyu izler** (440 ↔ 640 yalnız varsayılan ölçüde, sağdaki komşu kayar) ve şema 8 göçü DOM'a dokunmadan; paftanın boyu satırlardan (tavan %55, taban iki satır); göbekte satır numarası yalnız tablo açıkken; Enter alttaki satırın aynı sütunu; pafta mousedown'ı yutar; ad düğmesi paneli AÇAR; **tablo bir kanvas bileşeni DEĞİL** (tip · palet · panel yok) |
| `tests/e2e/fead-tablo.spec.js` + `fead-sihirbaz-tablo.spec.js` | Kayış Tablosu — Pafta (gerçek tarayıcı) | **Pafta**: geometri kartının çiziminin altında ve çubuğun üstünde (işletme kartında yok); gerçek `63,5` yazımı modeli ve çözümü değiştirir; Sekme ve Enter odağı düşürmüyor; satır oku, sürücü kilidi, yön düğmesi (efektif çap +0,2 mm), sil/ekle; **Tablo düğmesi** kartı 640 ↔ 440 çeviriyor ve komşu kayıyor; fare/odak/seçili satır CSS'ten, çözüm sütunu oyuk, satır ↔ iki çizimde işaret; **1366×768 · 1920×1080**: tuval kısalmıyor, kartlar görünür, tablo kartı geniş, yatay kaydırma yok, **hiçbir hücre içeriğini kesmiyor**, yazı çizimin ad etiketiyle orantılı (1920'de ≥ 12 px); hücreye basmak kartı taşımıyor, tekerlek listeyi kaydırıyor, eklenen satır görünür; arka plan kaydından sonra tablo yerinde, ana topolojide tablo yok; Ctrl+Z örneği TEK adımda geri alıyor ve açılış kartı kalıyor. **Sihirbazın 'Modeli Kur'u**: 6 kasnak + iki kanvas (açılışın boş kartı DEVRALINIR) + kayış + çözücü + rapor + sihirbaz = 12 düğüm, 0 tel, uyarı yok, tabloda 6 satır ve 6 yön düğmesi, kayış boyu kartın rozetinde, çözüm önizlemeyle birebir |
| `tests/unit/fead-cizim-masasi.test.js` | `js/cp-fead.js` Çizim Masası | **Kasnak çizimde seçilir, taşınır, eklenir**: ters köprü (ekran → mm) isabet halkasını kasnağın KONUM GİRDİSİNE döndürüyor (gergide `cenX/cenY`); etkileşim katmanı yalnız kartta ve CSS'siz belgede GÖRÜNMEZ; fare altı iki çizimde sınıfla, kart kurulmadan; ok tuşu TAM adım (ızgaraya yuvarlamaz), kayışı koparan adım yazılmaz; açıklık seçimi ve halkanın içi/dışı bağımsız kâhinle (aynalanmış düzen dâhil); ekleme adayı KOPYADA çözülür, gergi ↔ sürücü açıklığı kapalı, ekleme ve tablonun ekleyicisi TEK geri-al adımı; **kayış çizimde tıklanır** (2026-09-26) — isabet yolu kasnak halkalarının ALTINDA ve görünmez niteliklerle, raporun çiziminde yok; tık kayışı seçer, pencereyi açar ve iki çizimde işaretler; `veFeadKayisGaranti` yoksa kurar (seçimi bırakmaz), varsa dokunmaz; kayış SİLİNMEZ (genel silme yolu ayıklar, pencerede çöp kutusu yok); **çekmece emekli, tablo kartın Paftası** — çizim dışına bırakılan kasnak TEK geri-al adımında geometri kartının tablosunu açar, ekleyici kartın tablosunu tazeler, `#ve-fead-tablo` hiç kurulmaz |
| `tests/e2e/fead-cizim-masasi.spec.js` | Çizim Masası (gerçek tarayıcı) | Sürükleme imleçle birlikte yazar (0,1 mm ızgara, künye ve iki açıklık boyu ekranda) ve TEK adım; koparan konum yazılmaz, sebebi şeritte; tık pencereyi açar ve iki çizimde işaretler; klavye gergide avara merkezini tam adımla oynatır; paletten bırakma açıklığa girer (iz hedefi söyler), kapalı açıklık kırmızı ve reddedilir, boşluk reddedilir, çizim dışı tabloya gider ve satırı Paftada görünür; **KAYIŞA TIKLA** — palette ve kanvasta kayış kutusu yok, gerçek fare görünmez isabet yolunu alıyor (el imleci, hale), tık Kayış Özellikleri'ni açıyor ve kartı sürüklemiyor, iki çizimde işaretli, Delete kayışı silmiyor ve sebebini söylüyor, paftanın künyesi de açıyor; **gerçek Ctrl+Z**: katman geri alınınca kasnaklardan önce gelen kart boşalmıyor, ok tuşu + Ctrl+Z iki tur üst üste koordinatı geri getiriyor |
| `tests/e2e/kart-yuzey.spec.js` | `js/node-resize.js` + `js/ui-core.js` | **İki ortak kural, sentetik kartla**: tutamak sürüklemesi tipin en küçük ölçüsünde duruyor; kart içindeki TAŞAN liste tekerleği önce alıyor, taşmayan kap almıyor |
| `tests/unit/tuval-cihaz-pikseli.test.js` | `js/canvas-space.js` `veCanvasTransformCss` + `js/ui-core.js` `updateCanvasTransform` | **Çizilen kamera cihaz pikseline oturur**: çeviri × dpr tam sayı (1× · 1,25× · 1,5× · 1,75× · 2×), en çok yarım cihaz pikseli oynar, hizalı ofset değişmez, ölçeğe dokunulmaz; DURUM yuvarlanmaz (0,25'lik pan adımları birikir); ızgara içerikle aynı kamerayı izler |
| `tests/unit/fit-view.test.js` | `js/ui-core.js` `veFitViewToContent` | Seçeneksiz çağrı eski davranış; `only` süzülmeyeni kadrajdan çıkarıyor; kap çok küçükse kamera oynamıyor; `bottomInset` YOK (tablo kartın içinde; tuvali örten bir şey yok) |
| `tests/unit/minimap-ortmez.test.js` | `js/minimap.js` | **Minimap içeriği örtmez**: kamera çevirisi (ekran = (yerel − 3000)·zoom + ofset, parça 6 px pay kadar büyür), kutusuz düğüm (FEAD kasnağı) sayılmaz, modül kartının adı taşmaz, taşan ad parçaya girer (sınır çerçevesiyle aynı kural), çerçeve notu halka (boş içine düşebilir, kenarına düşemez), yazı notunun genişliği metinden; açık kutu içeriğe değince iner ve köşe boşalınca açılır, ölçü AÇIK hâlin kutusuyla (her denetimden sonra bakılır — titreme tek sayıda denetimde görünmez), oto iniş tercihi yazmaz, inmiş kutu çizim döngüsünde de denetlenir, sürüklerken durum değişmez; düğme oto inişi köşe boşalana dek geri çeker, kullanıcının indirdiği kutu inik kalır, açılışta tercih okunur; tuvalin kabı gözlenir ve gözlemci denetimi yeniden koşar |
| `tests/unit/pencere-ailesi.test.js` | `index.html` + `viewer/index.html` + `js/*.js` pencere kurucuları + `css/styles.css` | **Pencere ailesi tek**: her pencere kapatı çizgi ikon (✕ yazı karakteri ve elle SVG yok, tarama ≥ 14 düğme görüyor), iki kapat sınıfı aynı ölçü ve klavye odağı, eski başlık dilleri (`.ve-help-head/-close`, tablo kapatının görünümü) kalmadı, ortak başlığı kuran beş kurucu, başlık yazısı kabuk bandınınki, Çözücü jeton konuşur (perde, gölge, tek çizgi), **başlık ikonu şeritteki girişinin ikonu** (şerit `js/ribbon.js`'ten okunur, ikinci liste yok), her başlık çizgi ikonla başlar (emoji yok) |
| `tests/unit/cumle-duzeni.test.js` | arayüz etiketleri (kabuk: `index.html`, `viewer/index.html`, şerit, komut paleti, sağ tık, Ayarlar, Program Durumu, Kısayollar, Komuta, Sonuçlar kabuğu, içe aktarma, görüntüleyici panosu) | **Cümle düzeni (karar 9·B)**: kuralın kendisi (Başlık Düzeni → cümle düzeni; bileşen adı, yüzey adı, kısaltma, tuş, marka ve "Alt" gibi iki anlamlı kelime örnekleri), bileşen adları `components.js`'ten okunuyor, tarama boşa çalışmıyor (> 400 etiket), her çok kelimeli etiket cümle düzeninde, yüzey adı küçültülmüyor |
| `tests/unit/version-embed.test.js` | `tools/version-info.js` + `build.js` | **Sürüm künyesinin üreticisi**: PR numarası/başlık ayrıştırması, ÇOK SATIRLI merge gövdesinde kayıt ayırıcı kapısı (satır bazlı ayrıştırma changelog'u sessizce başlıksız bırakıyordu), git'siz dizinde boş künye, gömülen JSON'da ham `</script` kalmaması, favicon'un izinli listede DEĞİL data URI olarak gömülmesi |
| `tests/unit/deploy-status-offline.test.js` | `js/deploy-status.js` + `js/status.js` (`file://`) | **İndirilen tek dosya ağa ÇIKMAZ**: version.json + api.github.com yoklaması yok, 15 dakikalık zamanlayıcı kurulmuyor, nokta gömülü künyeyi gösteriyor (yeşil "güncel" DEĞİL), "Son Güncellemeler" ağsız doluyor. Karşı kapı `deploy-status.test.js` içinde: http(s)'te ağ yolu hâlâ çalışıyor |
| `tests/unit/fead-katman.test.js` | `js/cp-fead.js` katmanlar + `js/components.js` | **Kart ne çizeceğine kendi karar verir**: liste TEK KAYNAK — her katmanın çiziciye giden seçeneği var, çizici onu OKUYOR ve kart kurucusu bayrağı GEÇİYOR (üçünden biri kopunca kutucuk panelde görünür, çizimde hiçbir şey yapmaz); iki ÖN AYARIN varsayılanı farklı ve tip döneminin aynısı; **eksik anahtar varsayılana düşer** (eski kayıt çıplak şemayla açılmasın); tanınmayan ön ayar adı da varsayılana düşer; bağlı katman (adlar kapalıyken "adı kısalt"); seçim kart başına ve öteki kartın alanı bile açılmıyor; `saveState` mutasyondan ÖNCE; **ön ayar ADIYLA yazılır** (`katOn`), sekiz bayrağın kopyasıyla değil, ve elle seçimleri temizler; **ön ayar DEVRİ de belirler** (geometri `off`) ama yazılmış bir alanı EZMEZ ve söz söylemediği alana (titreşim · kol konumu) dokunmaz; varsayılan ön ayar `kat` ile `katOn`u SİLER; panel yalnız açık kart için basılır ve açıklık MODELDE DEĞİL; her ön ayar için bir düğme var, açık olan BASILI ve elle kutucuk oynayınca basılılık düşüyor; **`fead-run` diye bir tip YOK** — `componentDefs`te de, `index.html` paletinde de, `components.js` kodunda da; **kol konumu kart başına** (`veFeadPosModeShared` KALDIRILDI); iki kart aynı tipten ama iki ayrı resim çiziyor; **her bayrak çizimi gerçekten değiştiriyor** (kural, liste değil); adlar kapalıyken dar kartta yer de ayrılmıyor (240×180'de yarıçap 17,0 → 25,0); görünüm CSS'te ve durum kuralları var |
| `tests/e2e/fead-kanvas.spec.js` | `js/cp-fead.js` Kayış Yolu kartı (gerçek tarayıcı) | **BANT YOK, DENETİM ÜSTTE YÜZER**: kart dört yatay bant taşıyordu (iki seçici şeridi 44 px + durum 20 px + titreşim açıkken 20 px daha) ve her biri yüksekliğini ÇİZİMDEN alıyordu — 440×500 kartta çizime 436 px kalıyordu, titreşimle 416. Node'da hiç koşmayan halkalar: çizimin GERÇEKTEN kabuğun tamamını alması (**tablolu kartta** çizim + Pafta + çubuk satırı kabuğu tam dolduruyor ve birbirine binmiyor, gül ve titreşim şeridi tablonun altında kalmıyor; tablosuz kartta çizim kabuğun tamamı), yüzen çubuğun TEK SATIR kalması ve taşmaması (`left:50%` ile ortalanırsa mutlak kabın sığdırma genişliği yarıya iniyor ve çubuk dört satıra sarıyor — ölçüldü, 40 → 110 px), **yön gülünün çubuğun altında kalmaması** (ölçülmüş hata: gül TAMAMEN görünmez oluyordu; çözüm çizimi küçültmek değil gülü yukarı almak), titreşim şeridinin de yüzmesi ve çizimi küçültmemesi, rozetin BİRİNCİL okumasının duruma göre değişmesi (iyi hâlde tek sayı + soluk künye, kötü hâlde arızanın cümlesi ve künye YOK) ve renginin CSS'ten gelmesi; **çubuğun seçicileri kesilmiyor** — liste SEÇİLİ metne göre boyutlanır (`field-sizing:content`), kol listesi kısa adla (72 seçicide 25 → 0) |
| `tests/unit/fead-panel-dili.test.js` | `js/cp-fead.js` panel yardımcıları + `css/styles.css` `.ve-fp-*` | **FEAD pencerelerinin ORTAK DİLİ** (kullanıcı: *"yazılar kaymış, girdiler yanlış konumlanmış"*, ekran görüntüsüyle). Ölçülen beş kusurun DÖRDÜ yapısaldı: pencere geniş ama içerik tek sütun, aynı pencerede İKİ etiket modeli (`_feadSelect` sol-sağ ↔ `_feadGrid` üst-alt), etiket `center` ↔ giriş `right`, ve görünümün 240 satır içi `style=` dizesinde olması — satır içi CSS DURUM İFADE EDEMEZ, yani hiçbir FEAD panelinde `:hover`/`:focus` YOKTU. Kaldıraç altı YARDIMCIDA (44 kart · 18 ızgara · 13 liste · 74 açıklama hepsi oradan geçiyor): imzalar korunup yalnız üretilen işaretleme sınıf tabanlı yapıldı, bütün FEAD panelleri tek yerden düzeldi. Kapılar: alan satırı TEK kapta (etiket + giriş), sütun sayısı hücre sayısını AŞAMAZ (aşınca boş hücre doğuyordu), satır içi hiza/renk yok, birim ETİKETTE; **sekmeler** (kasnak 4 · avara 2) ve yalnız açık gövdenin görünmesi; **sekme durumu MODELDE DEĞİL** — kural 15'in aynısı — ve ölçüm ÇİZİM yolunu da kapsıyor (yalnız `veFeadPanelTab`i ölçen kapı, sekmeyi kurulum sırasında `node.data`ya yazan kodu GÖRMÜYORDU), ama durum bir yerde duruyor (yeniden kurulunca aynı sekme) ve geçersiz sekme İLK sekmeye düşüyor; **sağ sütun sekmeden BAĞIMSIZ** — aynı DOM nesnesi kalmak YETMEZ, gövdelerin DIŞINDA ve `hidden`sız olduğu ayrıca ölçülür (sekme gövdesine kapatılan bir sütun aynı nesneyi bırakıp tasarımın bütün katkısını yok ediyordu); kapılar TEK `veFeadChecks` çağrısından (kural 16) ve sebep `title`da; çözüm yokken sağ sütun SESSİZCE boş değil, sebebini yazıyor; **türetilen sayılar tablonun satırından** (kural 9) — bu halka STUB'LANAMAZ (`veFeadTableRows` cp-fead.js'in kendi modül kapsamında, çıplak çağrı `global`e hiç bakmaz), bu yüzden GERÇEK model çözülüp panelin bastığı değer satırın kendisiyle karşılaştırılıyor + dış çap negatif kapısı; ve dört panelin (kasnak · gergi · kayış · çözücü) DÖRDÜ de aynı dili kullanıyor; **pencere düzeni** (2026-09-23): özet ÇİPLERDEN (asılı ayraç yok), bölüm notu kodun iç adını taşımaz, türetilenler iki sütun, küçük resim pencerenin kasnağını VURGULAR (açı yok, çift çerçeve yok) ve adlar birbirine binmez (bütün örnekler × bütün kasnaklar); **veri tabloları açılır pencere BİRİMİNDE** (her `.ve-fp-duty` bir `data-ve-tablo` içinde, satır ekleyen düğme ve %zaman uyarısı birimde, çevrim seçici dışarıda ve alan dilinde, tablo düğmeleri sınıftan) |
| `tests/unit/fead-sekme-durum.test.js` | `js/cp-fead.js` `veFeadSekmeDurumlari` + `veFeadSekmeTazele` + `js/state.js` kayıt kancası | **DURUMLU SEKMELER** (kullanıcı: *"kullanıcı bu kısmı görmeyebilir ve eksik bilgi girebilir"*). Asıl kapı ANLAŞMA: on dört zorunlu alan (kasnak konumu/çapı, gergi merkezi · kolu · yayı, kayış kanalı) AG00976'da tek tek boşaltılıyor ve köprünün hatası/uyarısı ile sekmenin "eksik"i BİRLİKTE ölçülüyor; kayış boyu iki yönden (gergi varken kilitli serbest → hata YOK; gergisiz sabit kip → hata VAR). Devir sınırı sayımı kapının gördüğüyle aynı ve **katalogtan gelen sınır dolu sayılıyor**; sürücü KÖPRÜNÜN seçtiği (işaretsiz de), iki sekmesi `yok` ve ALAN SORMUYOR, işaret geçince geri geliyor; güç eğrisi **sessiz sıfırı** yakalıyor (köprü o satıra 0 kW yazıyor, eğri girilince dolduruyor); çözücünün dört sekmesi (motor devirleri · model · çevrim · sonuç) ve araç pencerelerinde durum YOK; bant gövdelerin DIŞINDA, rozet kalktı; **yerinde tazeleme** girişleri yeniden kurmuyor, `saveState` onu çağırıyor; bantın bağlantısı ilk BOŞ girişe odaklanıyor. Sekiz mutasyonla düşebildiği ölçüldü |
| `tests/e2e/fead-sekme-durum.spec.js` | Durumlu sekmeler (gerçek tarayıcı) | **Node'da hiç koşmayan halkalar**: gerçek tıklama bantın bağlantısıyla sekmeyi açıp imleci ilk boş alana koyuyor, GERÇEK KLAVYE (yaz + Sekme ×3) `onchange` → `veFeadSet` → `saveState` → `veFeadSekmeTazele` zincirini koşturuyor ve durum "tamam"a dönüyor, bant kalkıyor, girişler yeniden KURULMUYOR (iz korunuyor) — kayıt kancası sökülünce durum "eksik"te kalıyor (ölçüldü); sürücüde iki sekme "kullanılmaz" ve alan yok; şerit 380 px müfettişte dört pencerede TEK SATIR ve taşmıyor; durum yazısı bant zemini üstünde iki temada ≥ 4,5:1 |
| `tests/e2e/fead-katman.spec.js` | Katman paneli (gerçek tarayıcı) | **Node'da hiç koşmayan halkalar**: düğme yüzen çubuğun içinde ve çubuk kaymıyor, gerçek tıklama paneli açıyor, panel çubuğu İTMEDEN çizimin üstüne biniyor ve kartın içinde duruyor, kutucuk çizimi değiştiriyor ve **panel açık kalıyor** (kapansaydı ikinci kutucuk işaretlenemezdi), bağlı satır pasifleşiyor, `:hover` ve `:has(input:checked)` gerçekten hesaplanıyor, toptan işlemler sayacı 5/8 → 0/8 → 8/8 gezdiriyor; **ÖN AYAR DÜĞMESİ kartı gerçekten çeviriyor** (Geometri ↔ İşletme: animasyon yükü doğuyor/kayboluyor, basılı durum CSS'ten hesaplanıyor, elle kutucuk oynayınca hiçbir ön ayar basılı kalmıyor) ve Geometri'ye dönünce `kat`/`katOn`/`animRpm` alanlarının üçü de SİLİNİYOR; **iki kart iki ayrı resim** çiziyor, ikinci panel açılınca birincisi kapanıyor, her kart KENDİ kol konumunu çiziyor ve paletten kurulan ÜÇÜNCÜ kanvas da ön ayar seçebiliyor |
| `tests/unit/geri-al-yolu.test.js` | `js/state.js` `restoreState` (GERÇEK) + `js/connections.js` imza | **Yığındaki durum bir anlık görüntü**: geri-al ve ileri-al KOPYA geri yükler — ikinci Ctrl+Z da geri alıyor, geri yüklenen düğüm kaydın nesnesini taşımıyor; FEAD kartı kasnaklardan önce gelse de geri yüklemeden sonra TAM modelle kuruluyor (imzaya girmeyen bir alanın geri alınması kartı boşaltmıyor) |
| `tests/unit/state.test.js` | `js/state.js` | Undo/redo stack yönetimi; **toplu kurulum TEK adım** (`veStateBatch`) — sayaç, `finally` ile kapanma, açılış durumunun yığın TABANI olması (`veStateResetBaseline`) |
| `tests/unit/toolbar-save.test.js` | `js/toolbar.js` | Proje kaydetme, JSON serileştirme, showSaveFilePicker |
| `tests/unit/viewer-board.test.js` | `viewer/js/board.js` | Görüntüleyici panosu: bir panoda tek ölçüm dosyası kuralı, X ekseni seçenekleri, veri kapısı |
| `tests/unit/can-dbc.test.js` | `candbc/js/can-dbc.js` | **CAN Çözümleyici** DBC söz dizimi: uzatılmış kimliğin 31. bitten çözülmesi, çoklama (`M`/`m<n>`/`SG_MUL_VAL_`), `VAL_` tabloları, TIRNAK İÇİNDEKİ `;` deyimi bölmez, çok satırlı `CM_`, NS_ bloğundaki sembollerin deyim sanılmaması, bozuk girdide uyarı |
| `tests/unit/can-decode.test.js` | `candbc/js/can-decode.js` | **Bit düzeni** — programın en sessiz hata sınıfı. Intel (@1) ve Motorola (@0) için EL İLE HESAPLANMIŞ referanslar, Motorola adım kuralı (bit 0'da +15), iki tümleyen, IEEE float/double, ölçekleme sırası, kısa karede sınır dışı bitin 0 okunması, çoklama, `VAL_` tablosunun HAM değerle aranması |
| `tests/unit/can-log.test.js` | `candbc/js/can-log.js` | Beş kayıt biçimi (candump ×2, Vector ASC + CAN FD, PCAN-Trace v1/v2, BusMaster) gerçek satırlarla; **biçimler birbirinin satırını çalmaz**; kare deposunun tipli dizilere geçişi ve büyümesi; kimlik + çerçeve tipine göre dizin; zaman ölçeği; ÇÖZÜLEMEYEN SATIR SAYACI |
| `tests/unit/can-chart.test.js` | `candbc/js/can-chart.js` + `can-example.js` | Eksen adımı (1-2-5), imleç örneklemesinin ARA DEĞERLEMEMESİ, ondalığın çarpandan türemesi + **GİDİŞ-DÖNÜŞ**: bilinen fiziksel değer → kare → candump satırı → ayrıştırma → çözme → aynı sayı (Motorola ve çoklama dâhil) |
| `tests/unit/viewer-sync.test.js` | `viewer/sync.js` | Görüntüleyici kopyaları `js/`'ten geride kaldıysa kırmızı — sessiz ayrışmaya karşı kapı |
| `tests/unit/measure-dropzone.test.js` | `js/measure-dropzone.js` | Sürükle-bırak uzantı süzgeci (sessiz yanlış çıktıya karşı); **yerel bırakma alanı** (`data-ve-dropzone`) kaplamayı devralıyor — alan içine bırakılan dosya ölçüm sihirbazını açmıyor, alan dışı davranış korunuyor |
| `tests/unit/simulation-engine-grade.test.js` | `js/simulation-engine.js` | Yol eğimi işaret konvansiyonu (harita ↔ fizik çevirisi) + dinamiğin değişmediğini bağlayan altın değerler |
| `tests/unit/shot-tool.test.js` | `tools/shot.js` | Ekran görüntüsü aracının ayrıştırma çekirdeği: bilinmeyen bayrağın SESSİZCE yutulmaması (yanlış ekranın görüntüsü alınırdı), hedef takma adları, PNG ölçüsü, karşılaştırmanın İKİ GÖRÜNTÜYÜ TEK ÖLÇEKLE küçültmesi |
| `tests/unit/source-hygiene.test.js` | `js/`, `viewer/js/`, `candbc/js/`, `css/`, `index.html` | **Yapısal kapılar**: üst-seviye bildirim çakışması yok, kaynakta kontrol karakteri yok |
| `tests/unit/build-freshness.test.js` | `viewer/build.js` + `candbc/build.js` + `package.json` | **Git'e dâhil üretilen dosyalar taze mi**: `css/` üç ürüne birden girdiği için bir tema rötuşu `MFSim_Olcum_Goruntuleyici.html` ve `MFSim_CAN_Cozumleyici.html`'i birden bayatlatıyor ve kapı eskiden YALNIZ CI'daydı. Derleme geçici yola yapılır (`MFSIM_BUILD_OUT`) — test çalışma ağacını kirletmez. Ayrıca `three` köken kapısı: npm bağımlılığı kaldırıldı (kullanılmıyordu, kurulum başına 30,1 MB), sürüm izi `index.html` ile `vendor/three.min.js` arasında bağlı |
| `tests/unit/pchip-uc-kopya.test.js` | `js/numerics.js` + `js/ft-performance.js` + `js/mount-core.js` | **Tek PCHIP algoritmasının ÜÇ ayrı yazımı aynı eğriyi veriyor**: `assets/examples`'taki her sayısal tablo (13 motor tork eğrisi + konvertör K-faktörü/τ) üç kopyada 200 noktada karşılaştırılıyor. Ayrışma SESSİZDİ — her dosya kendi başına doğru olduğu için hiçbir davranış testi görmüyordu, ama aynı motor eğrisi Motor Freni modunda başka, Tam Gaz modunda başka tork veriyordu. Yakalanan kusur: `_pchipEndSlope` uç eğim kelepçesini `< 0` ile kuruyordu, diğer ikisi `<= 0`; MATLAB/SLATEC `pchipend`'in `sign(d) ≠ sign(del1)` testi `del1 == 0` iken ateşler ve kesin `<` onu KAÇIRIYOR → düz uç aralıklı tabloda eğri platonun altına sarkıyor, şekil koruma garantisi bozuluyordu. Sevk edilen veride `ap_isb340_tc411` τ(SR) tablosunda canlıydı — etkisi orada ~3e-5, ama düz platolu bir tork tablosunda 7–35 N·m (ölçüldü). **Bilerek ayrık üç nokta ÇİVİLİ**: tablo dışı (A/B düz — C doğrusal, gerekçesi `mount-core.js:301-304`), n=2 (A `null` döner ve `veEvalPchip` sessizce `0` verir), geçersiz girdi (yalnız B adresli hata atıyor) |
| `tests/unit/loader-splash.test.js` | `js/loader.js` + `index.html` açılış ekranı | **Açılış ekranı (AMBLEM)**: splash gövdesi ↔ `ELS` kimlik sözleşmesi (bir yeniden adlandırma cetveli sessizce durdururdu), `data-mfsim-stage` işaretlerinden öbeklerin kurulması (işaretsiz script bir öncekine yazılır; ad `&` içerebilir) ve **yalnız o anki öbeğin** Roma rakamıyla yazılması (rakam sıradan türer: IV, IX, XIV), etiketin yalnız öbek DEĞİŞİNCE belirmesi, atlanan modülün GÖRÜNÜR olması (uyarı satırı + öbek sürerken kehribar rakam) + yüklemenin devam etmesi, sürüm künyesi (modüler kopyada boş kalır), ipucu döngüsünün kapanışta DURMASI; **SAHNE**: kare listesinin defer OLMAMASI ve yükleyiciden ÖNCE gelmesi (defer setine düşerse açılış fotoğrafsız açılır), karenin seçilip boyanması + gömülü data URI ↔ dosya yolu, kare yoksa kâğıt zeminde açılıp yüklemenin yine bitmesi, **perdenin TEMANIN ZEMİNİNDEN türemesi** (tema-nötr siyah çıplak renk kapısından geçer ama açık temada koyu yazıyı koyu zemine oturturdu) ve fotoğrafsız hâlde hiç çizilmemesi, amblemin kapanışta kaymaması ve karşılama kartıyla aynı sol kenarda durması, modül giriş panelinin karşılama kartıyla BİREBİR geometrisi (left/genişlik/yarıçap/cam/gölge), **temanın ilk karede** uygulanması (kayıtlı tema hiçbir şey çizilmeden konuyor, eski kimlik ilk karede göçüyor, bozuk kayıt varsayılana düşüyor); **KADEME**: kademe sayısı tek kaynak (CSS jetonu `--mfsim-kademe`, loader onu OKUR ve cetvelin ızgarası da onunla çizilir — kademe başına bir bölme), ilerlemenin kademeye yuvarlanması (1/24'te cetvel hâlâ boş), son geçilen bölmenin VURGU taşıması ve bitişte vurgunun sönmesi, yüzdenin GÖRÜNMEMESİ (değer `aria-valuenow`da), dişlinin kademe başına bir çentik dönüp %100'de TAM TUR tamamlaması (devir teslimde dik durur), modül adının da kademeyle değişmesi, süsleme karelerinin (dönen dişli + parıltı) KALDIRILDIĞI |
| `tests/unit/welcome-screen.test.js` | `js/components.js` + `index.html` + `css/styles.css` karşılama | **Karşılama ekranı**: kabuk sınıfının ÜÇ kaba birden yazılması (şerit `.ve-main` dışında), sürüm künyesi `window.__MFSIM_BUILD`'den (modül yükleme sırasına bağlı DEĞİL), markup sözleşmesi (dört `.ve-module-card`, E2E bu seçiciyle tıklıyor; ad + künye var, uzun açıklama YOK), **P4 · M6 yapısı** — modüller sol panelin içinde, slayt karşılamanın DOĞRUDAN çocuğu, sağ sütun kalkmış —, tek yazı tipi (mono yok), son değişiklikler panelinin YERİNDE açılması + HTML kaçışı, güncellik satırının DÜRÜSTLÜĞÜ (yerel kopya "Güncel" demez), açılış koreografisinin jetonlardan türemesi, kart animasyonunun `backwards` doldurması (`both` hover'ı sessizce öldürüyordu), uçan kopyanın asılı kalmaması |
| `tests/unit/karsilama-slayt.test.js` | `js/components.js` slayt sürücüsü + `assets/karsilama/` + `css/styles.css` | **Karşılama slaytı**: künye ↔ klasör iki yönlü eşleşme (öksüz dosya ve hayalet kayıt yok), kare/klasör boyut tavanları, kaynak seçimi (gömülü data URI ↔ dosya yolu), karıştırmanın gerçek permütasyon olması, karşılama kapanınca zamanlayıcının DURMASI, kabın taşmayı kırpması, panelin slaytın üstünde kalması, **reçete jetonları** (örtü %0 · resim %100 · keskin · cam %25, backdrop-filter YOK, color-mix'siz tarayıcıda panel OPAK) ve **netlik/tempo**: kare kutusundan büyük değil (`inset` negatif olamaz — eski −%3 netlikte %14,3'e mal oluyordu), Ken Burns 1'den BAŞLAR (kare geldiği an en net), kareye SVG süzgeci bağlanmamış (ölçülmüş reddediş: keskinleştirme görüntüde işe yarıyor ama animasyonlu elemanda 16,7 → ~295 ms/kare), bekleme (js) > geçiş (css) ve Ken Burns karenin ömründen uzun; **açılış karesi devamlılığı**: karılan sıra açılış ekranının gösterdiği kareyle BAŞLAR (yoksa devir teslimde fotoğraf değişir), ilk kare geçişsiz açılır (perde kalkarken kare yarı saydam kalmaz) ve devir teslim slaytı YENİDEN KURAR — zamanlayıcı 11 sn, yükleme ~13 sn, yani slayt perdenin arkasında bir kare ilerliyordu; **6·2 ekrana göre süzgeç**: ölçü haritası listeyle bire bir ve her ölçü dosyanın webp başlığıyla aynı, 1920×1080'de kalanın hepsi ≤ 1,25 / elenenin hepsi > 1,25, her kare büyüyorsa eşik en keskin kareye göre ölçekleniyor, ölçüsü bilinmeyen kare kalıyor, slayt aynı süzgeçten geçiyor |
| `tests/unit/karsilama-secici.test.js` | `tools/karsilama-secici.{js,html}` + `tools/karsilama-kunye.json` + `assets/karsilama/` | **Seçim tahtasının kaynak üçlüsü** (üretilen sayfa git'e dâhil değil, yani onu tutacak tazelik kapısı YOK): künye ↔ klasör iki yönlü — künyesiz kare üreteci DURDURUR ama **kaldırılan karenin geride kalan künyesi hiçbir şeyi durdurmaz** ve olmayan bir dosyayı anlatmaya devam eder; grup değeri şablonun kendi tablosundan doğrulanır (tanınmayan grup kareyi sayfada HİÇ çizmez, 28 bekleyip 27 görmek gözle yakalanmaz); `es` (benzer kare) ipucu var olan bir kareyi göstermeli ve KARŞILIKLI olmalı; şablon ↔ üreteç veri çapası; ve şablonun kendi başına da `.perde[hidden]`'ı kapatması — `.perde{display:grid}` sınıf seçicisi tarayıcının `[hidden]` kuralını eziyor, yayın kabuğundaki `!important` bunu MASKELİYOR |
| `tests/unit/results-txt-preview-download.test.js` | `js/results.js` | TXT önizlemesinin "HTML İndir" yolu — iki rapor üreticisinin ayrı kaldığı; düğme kablolaması artık ÜRETİLEN YÜZEYDEN ölçülüyor (kopya sayısı değil: bandı tek üretici kuruyor) ve dört panelin de aynı kabuğa gittiği |
| `tests/unit/results-txt-page.test.js` | `js/results.js` + `css/styles.css` | **TXT raporunun görünümü**: üst bandın soldaki "Veri Gezgini" bandıyla TEK ölçü kaynağından beslenmesi (yükseklik, alt çizgi, başlık puntosu, zemin, düğme sınıfı), sayfanın A4 olması ve içeriğe göre DARALMAMASI, gövdenin tek `<pre>` kalması (blok blok ortalama yok) ve metnin bire bir korunup kaçışlanması, font ölçüsünün sayfaya sığmaktan türemesi + okunur tavan, karakter oranının ölçülemeyince GÜVENLİ tarafa düşmesi, indirilen belgenin aynı sayfayı açıp `@page{size:A4}` ile basması |
| `tests/e2e/fead-yerlesim.spec.js` | `js/cp-fead.js` `veFeadFallbackSlots` + `veFeadArrangeByCoords` | **Örnek yüklenince kartlar nereye düşüyor** (gerçek tarayıcı): tablo ÜSTTE, iki kanvas YAN YANA, hiçbir kart çakışmıyor ve blok geniş — dar-uzun sütun dizilişi sığdırmayı yükseklikten sınırlayıp zoom'u 0,473'e düşürüyordu. **İKİNCİ HALKA YEDEK YOL**: `window.veFeadArrangeByCoords` kırılıp aynı kapılar yeniden ölçülüyor — kurucular yerleştiriciyi `try/catch` ile sarıyor, yani o yol patlarsa geçerli kalan sıra yedektir ve eskiden kanvasları ALT ALTA diziyordu. Node'a TAŞINAMAZ: yerleştirici aynı modül kapsamında duruyor, stub'lanamıyor ve koordinatların üstüne hemen yazıyor |
| `tests/e2e/app.spec.js` | Tüm uygulama | Sayfa yükleme, menüler, bileşen ekleme, kaydetme |
| `tests/e2e/measure-import.spec.js` | İçe aktarma sihirbazı | Gerçek .xlsx → sütun tarama → X/Y seçimi → şeritler |
| `tests/e2e/viewer.spec.js` | `MFSim_Olcum_Goruntuleyici.html` | **Üretilen tek dosya**, `file://` üzerinden: açılış, içe aktarma, sürükle-bırak, birleştirme, tema, sıfır ağ isteği |
| `tests/e2e/results-txt-page.spec.js` | TXT rapor önizlemesi (yerleşim) | **GERÇEK tarayıcı**: iki bandın AYNI yerde bitmesi (ölçülen eski fark 12 px), başlıkların aynı punto, sayfanın 794 px = A4 olması, metnin tek blok / tek sol kenar kalması (eski: 43 blok, 10 kenar), 119 sütunluk tablonun sayfaya sığması (yatay kaydırma yok) ve dar raporun tavan puntoyla açılması. Rapor METNİ sahte — ölçülen şey kabuk; bu halkalar Node'da HİÇ koşmuyor |
| `tests/unit/fead-wizard.test.js` | `js/cp-fead-wizard.js` + `js/components.js` + `js/cp-fead.js` | **Başlangıç Sihirbazı**: bileşen sözleşmesi (0/0, `maxInstances:1`, palet + kayıt defteri + panel dağıtımı + çift tık + modal kabuğu + CSS sınıfları), betiğin `cp-fead.js`'ten SONRA yüklenmesi, başlangıçta İKİ açılış yüzeyi; **durum → düğüm çevirisi iki örneği de BİREBİR geri üretiyor** (AG00976 L 1714,6 · T 544,05 · rel 28,075 · BMC L 1715,0 · T 532,142 · rel 28,4271) ve duty kW anahtardan kimliğe çevriliyor — çevrilmezse sonucun gerçekten değiştiği İKİ UÇTAN tutuluyor; gerginin TEK koordinat taşıması (kasnak merkezi ve kip alanı YOK, kayış boy kipi yazılmaz — boy yapısal olarak çıktı); sıra/sürücü/satır kuralları (ilk kasnak sürücü doğar, sürücü tek, gergi sıraya kendiliğinden girer, kasnak silinince duty sütunu da düşer, tip değişince temas tarafı tipin varsayılanına döner, sırayı çevirmek yönü çevirir); kurulum kapısı (mevcut kasnakla kapalı + sebep, onayla açık) ve kurulum (6 kasnak + 6 tel, kW göçü, **kurulan model önizlemeyle birebir**, sihirbaz düğümü KALIR / örnek düğümü GİDER, temizle işaretliyse teller de gider); adım eşlemesi, yedi adımın boş ve dolu durumda `undefined`/`NaN` basmadan çizilmesi, taslağın düğüme yazılıp KALDIĞI YERDEN açılması ve **form içi düzenlemenin `saveState` çağırmaması**; **gergi satırı** (2. adımda sanal olarak var, sürücü olamaz ve silinemez, X/Y'si gerginin **montaj konumu** — `cenX/cenY`'ye ASLA yazmıyor — ve hangi nokta olduğu çipte yazılı, satırın yazdığı yer 4. adımın okuduğu `st.ten`'in ta kendisi); **künye etiketi** tek üreticiden (`veFeadTenLabel` → `kol 90 mm · 22.07 Nm`), 14 kayıt 14 AYRI etiket, panel de aynı üreticiyi kullanıyor ve künye uygulaması `veFeadTensionerApply`'a devrediyor — kendi beyaz listesi `tenPart`/`inertia`'yı yutuyor ve kodsuz künyede eski kodu bırakıyordu (pim *"kod yok"* diyordu, oysa kayıtta kod VAR); **katalog önerisi** bloğunun bulunmaması; **aksesuar modelleri** (duty kW hücresi GİRDİ değil okuma, `_fwKwEff` önceliği köprüyle aynı — duty kW > kasnak eğrisi > katalog > 0 — model seçince kayıtlı kW'ın TEMİZLENMESİ ki katalog devreye girsin, ölçüldü: ALT 3,61 → 7,62 kW, ve güç kaynağı olmayan yük taşıyıcının adıyla uyarılması); **özette kol açısı künyesinin OLMAMASI**; **adım rayının üç durumu** (`veFeadWizStepState` köprünün KENDİ listesinden türüyor, hata uyarıyı bastırıyor, boş sihirbazda `st-err` / dolu örnekte yedi `st-ok` + `✓` rozeti, yedi adımın yedisi de durum sınıfı ve rozet taşıyor — sessiz adım yok — ve DURUM ile ODAK ayrı kanallar: konumsal `done` emekli); **alt çubuk dolgusunun** modal başlığının bandını aşmaması; **üst bölge kabuk bandı kadar** — başlık ve alt çubuk `--bant-h`'tan (dikey pay 1 px, `border-box`; kapat ve alt çubuk düğmeleri bant düğmesiyle tek boy), rayda pencere başlığını tekrarlayan marka bloğu YOK (öksüz CSS kuralı da yok), adım başlığı tek satır | · **ikinci tur**: gergi satırının TİP sütunu bileşenin ADINI basıyor ve künye seçici ONUN ALTINDA, ad yer tutucusu ile kurulan düğümün `customName`i AYNI üreticiden (`_fwTenAd` → `Otomatik Gergi`, palet adı "Gergi" iken); **dönüş yönü** seçimi durum TUTMAZ (sırayı çevirir, `st`'ye `spin/dir/yon` yazılmaz), aynı yönü seçmek hiçbir şey yapmaz, geometri BİREBİR sabit (L ve rel 6 hane), iki adım AYNI üreticiyi basıyor, yön okunamazken düğmeler kapalı ve CW ↔ CCW aynı renkte; **künye kilidi** (`readonly`, `disabled` değil — sayı okunur kalır), montaj konumu ASLA kilitlenmez, "elle gir" kilidi açar ve künyenin değerlerini KORUR, satır ile 4. adım aynı okuyucudan, kilit çözümü değiştirmez; **kayış adımı** Profil/Marka/kanal DURUYOR ama Künye · Malzeme · kip seçicisi YOK, `beltDataMode` koşulsuz `none`, ve SORULMAYAN alan TAŞINMAYAN alan DEĞİL (örneğin `beltType`/`tolerance`/`wearPct`/`massPerRibKgM` kayış düğümüne gidiyor); **güç geometriden bağımsız** — koordinatsız modelde katalog kW'ı geliyor ve devirle değişiyor, oran bile kurulamıyorsa sebep AYRI (`cozumsuz` ≠ `yok`), çözülmüş modelde `ratioSys === sys`; **girdiler topolojiye ulaşıyor** — `accPreset` düğüme yazılıyor VE bileşenin kendi panelinde `selected` görünüyor, gergi tek koordinat taşıyor, kW sözlüğü kanvas kimliğine göçüyor, kurulan modelin çözümü önizlemeyle birebir; **serpantin sırası** — OKUNAN ve YAZILAN sıra birleşti: çevirme gerçekten çeviriyor (elle kurulmuş modelde gergi SONDA kalıyordu → L 1714,61 → 2459,29 mm), gergi satırının ok düğmeleri artık ölü değil, ve iki kez çevirmek birim işlem
| `tests/unit/fead-catalogs.test.js` | `js/fead-engines.js` + `js/fead-accessories.js` + `index.html` | **BMC motor ve aksesuar katalogları** (24 motor · 10 alternatör · 4 klima): anahtar tekilliği, devir sınırlarının sıralı olması, eğrilerin devirde KESİN ARTAN olması — sonuncusu bir çıkarma hatasının kapısı (defterin eğri bloklarının sağındaki 1,2,3… indis sütunu ilk çıkarmada eğriye sızmıştı); defter çıpaları (57RS303234 künyesi ve 12 noktalı tork/güç eğrisi, Prestolite 155A ve Valeo TM31 künyeleri); eksik alanın `null` olması (sıfır, olmayan bir kasnak iddia ederdi); uygulama sözleşmesi — katalogda `null` olan alan kullanıcının değerini EZMEZ, **boş eğri YAZILMAZ** (Prestolite 120A'nın devir/kW satırları defterde boş; yazsaydı kullanıcının kW tablosu sessizce sıfırlanırdı), eğri KOPYALANIR, `unlink` bağı çözer ama alanları bırakır, sınır kaynağı katalogtan FARKLI OLMAKLA belirlenir (yalnız "dolu mu" bakan bir tespit katalog değerlerini "elle girildi" sayıyordu); `fead-accessories.js` yükleme sırasında `fead-checks.js`'ten ÖNCE |
| `tests/unit/fead-transient.test.js` | `js/fead-transient.js` + `js/cp-fead.js` yük sözleşmesi | **Motor çevrimi senaryosu**. Ayırdığı şey ne türetildi/ne dayatıldı: devir geçmişi DAYATILMIŞ (volan ataleti yok) ve notlarda AÇIKÇA yazılı; türetilen yalnız rampanın şekli (α ∝ mutlak elde kalan tork — oran kullanan ilk yazım sessizce etkisizdi, faz içi α oranı 1,00 idi, mutlakla 1,46). Gerilme ızgara noktalarında çekirdeğin `peakEstimate`'iyle yükün yuvarlama bütçesi içinde BİREBİR yeniden kuruluyor (T = A + α·B tam doğrusal); ivme gerilmeyi artırıyor, yavaşlama azaltıyor. Dört sessiz tuzak kapılı: rölanti altında sabit güç kuvveti patlatıyor (ölçülen 41.927 N) / sabit tork duran motorda yük iddia ediyor (ölçülen 798,6 N) → kuvvet devirle orantılı ve durgun kayış tasarım gerginliğinde; faz sınırlarında devir sürekli; senaryo döngüsel. Süpürmede açıklıklar ateşleme mertebelerini gerçekten kesiyor (≥16 geçiş) ve aynı kesişme çıkışta/inişte FARKLI devirde (ivme histerezisi). **Yük attribute'tan sağ çıkıyor** — tek tırnaklı biçim senaryo metniyle 7573. karakterde kırılıyordu ve animasyon sessizce hiç kurulmuyordu; kapı içine tek VE çift tırnak koyarak zorluyor. Altı mutasyonla ölçüldü |
| `tests/unit/fead-card-design.test.js` | `js/cp-fead.js` kart sunumu + `js/fead-model.js` `veFeadSpanTensionMap` | **Kartın yeni sunumu**: ad ↔ sarım açısı çakışması (açı YUMUŞAK engel — gül çakışması artmıyor, iki modelde 64 gül konumu), kısa ad yalnız çizimde (panel/rapor tam adı basıyor, tam ad kartın tablosunda), künye tablosunun sayıları çekirdekle birebir (sarım çizilen konumun geometrisinden, devir ve güç `perPulley`'den), dar kartta tablo düşüp açıların çizime GERİ dönmesi, gerilme haritasının ankrajının ÇİZİLEN konumdan gelmesi (Serbest 260 N ↔ Mean 544 N), devir yokken harita olmaması, rengin gerilmeyle monoton olması, künyenin tek satır kalıp `ağır çekim` ve `KALİBRE DEĞİL` damgalarını taşıması. Gerilme sayısının okunurluğu ayrı kapılı: sayı çerçevenin İÇİNDE (kenetleme; kaynak vaka AG00879 @800 dev/dk 440×458'de "1231 N" taşıyordu), açıklığın DIŞ tarafında (sabit normal yarısını çizimin içine atıyordu), zemin renginde hâle + `--text-primary` ile yazılı (rampa amberi açık temada okunmuyor), ve ad yerleştiricisine yumuşak engel olarak giriyor. **İKİ KANVAS, TEK TİP (2026-09-11)**: ikisi de `fead-layout` ve `componentDefs['fead-run']` YOK; ayrım ÖN AYARDA (`data.katOn`) — geometri donuk (yük yok · gerilme haritası yok · sarım açıları var), işletme canlı (yük + harita · sarım açıları yok); **ön ayar değiştirilebiliyor** (geometri kanvası işletmeye alınıp geri dönüyor, varsayılan alanı SİLİYOR); DÖRT denetim de her kartta (kol · katmanlar · devir · titreşim — tip döneminde kartlara bölünmüştü); **kol konumu KART BAŞINA** (birininki ötekini çevirmiyor, ölçüt seçenek listesi değil ÇİZİLEN konum); tazeleme tek kapıdan üç kartı da kurar; örnek İKİ kanvas getiriyor ve ikincisi `customName` ile ayırt ediliyor; künye tablosu iki kartta da YOK (Kayış Tablosu yazıyor). On sekiz mutasyonla ölçüldü (biri eşdeğer: kuyruk tüketimi gözlenebilir değil) |
| `tests/unit/fead-vibration.test.js` | `js/fead-model.js` titreşim yükü + `js/cp-fead.js` deformasyon | **Titreşim animasyonu** (çırpma + burulma mod şekli). Ayırdığı şey iki farklı dürüstlük profili: MOD ŞEKLİ bir SONUÇTUR (özvektör) → şekil oranları çekirdeğin `torsionalModel` özvektörüyle 9 hane birebir; ÇIRPMADA frekans sonuç, GENLİK DEĞİL → genliğin kazançla tam doğrusal olduğu ve kaydırıcının kenetlendiği ölçülüyor. Frekans köprüde YENİDEN HESAPLANMIYOR (`spanFrequencies` ile 9 hane birebir), ateşleme silindir sayısından. Animasyonun tanı değeri kilitli: **en çok savrulan açıklık, ateşleme mertebesine en yakın olandır** (AG00686 @2750: ateşleme 137,5 Hz, CRK→IDR 150,1 Hz, büyütme 5,13 — dördün en büyüğü) ve büyütme sönüm tavanını aşmıyor. Ekran frekansı kinematiğin katsayısını PAYLAŞIYOR (oran birebir), tavanı aşarsa ek ağır çekim girip tavanı tutuyor. Deformasyon kapıları: `def` yokken diş ve kol yolları BİREBİR eski, çırpma açıklığa DİK ve uçlarda sıfır, kasnak yaylarına DOKUNMUYOR; mod şeklinde **kayış kasnakta kaymıyor** (yay üstündeki nokta yarıçapı ve özvektör açısını 6 hane koruyor), **açıklık ucu ile yay ucu aynı yere kayıyor** (kayış yırtılmıyor), gergi kasnağı kol yayı üzerinde kalıyor; kapalı çevrim deformasyonla da kapanıyor. Model çözülemezse `null` (sessizce sıfır şekil DEĞİL) ve şerit sebebi yazıyor; panel ile kart tek alan okuyor. Dört mutasyonla ölçüldü |
| `tests/unit/fead-step.test.js` | `js/step-p21.js` + `js/fead-step.js` + `tests/helpers/step-yaz.js` + `step-ornek.js` | **STEP'ten kasnak geometrisi**: okuyucu (`\X2\` adlar, dize/yorum içi ayırıcı, karmaşık varlık, inch/derece birimi, iç içe montaj dönüşümü, ters RRWT, MAPPED_ITEM uyarısı); tanıyıcı — dış çap kaburga TEPESİ (omuz tuzağı), tor tepesi yüzün içinde, tek yanak kanal değil, PJ/PK/PL adımı, düz kasnak = kayışı taşıyan yüzey (gövde avaradan çok yüzlü olsa da; boşaltılmış avara tek yüzey, orta flanş böler), yalnız üçgenli dosya, iki bakış yönü; **ROL KULLANICININ** (rolü test verir) — okuma kasnak aramaz, rolsüz çözüm hata; rol alt montaj düğümüne (gergi), bir parça tek birime (en yakın rollü ata); seçilmemiş kasnak analiz edilmez ve düzlemi bozmaz; düzlem EN ÇOK birimi oturtan konum (yanlış düzlemdeki rollü parça söylenir); iki izli damperde doğru iz; kaburgalı avara kanallı okunur; sürücüyü geometri değil kullanıcı seçer; **uçtan uca** Gates AG00686 STEP → örnek kaydı → köprü (span %0,5 · sarım 0,2°), ayna modeli çözer ama dönüş yönü ters |
| `tests/unit/fead-wizard-step.test.js` | `js/cp-fead-wizard.js` "STEP'ten başla" + `js/step-p21.js` `veStepP21Metin` + `tests/helpers/step-ornek.js` | **Sihirbazda STEP'ten başla**: bayt → metin (düz · BAŞLIK ALANLI gzip · zip içinde .stp seçimi · UTF-8/Latin-1 · bozuk gzip adresli hata); **rolsüz açılış**, hesap bir DÜĞME (rol vermek hesaplamaz, hesapsız aktarım yok), rol değişince sonuç düşer / bakış değişince düşmez; rol düğüme — ata ve torunların rolü düşer, kök satır değil; **kayış düzlemi çizimi** (hesaptan önce yok; kasnak dış çapıyla, krank orijinde, y yukarı, bakış çevirir, gergi kolu+pivot, kayış yolu YOK, renk CSS jetonundan); **GİDİŞ-DÖNÜŞ** (kural 20); **kayışa dokunulmaz** (6PK dosya da) ve çözücü boş durumdan; iz durumda; AG00686 künyeyle köprüde raporun açıklıkları; tek krank / tek gergi hesabı durdurur; orijin kullanıcının krankı; ayna; sıra uyarısı ve onayı; STEP ↔ künye farkı; başka düğümde eski dosya düşer; bırakma; **1. adımda TEK birincil düğme** |
| `tests/e2e/fead-step.spec.js` | Sihirbazın STEP kartı (gerçek tarayıcı) | Gerçek `File` (başlıklı gzip `.stpZ`) → 5 satır, HEPSİ rolsüz, hesap düğmesi kapalı; tabloda ve gövdede yatay kaydırma yok (1366×768); satırlardan gerçek rol seçimi → "Çap ve merkezleri hesapla" → çizimde 4 kasnak + gergi kolu, görünür boyutta, rengi temadan çözülmüş; rol değişince çizim düşer; aktar · Kasnaklar'da sıra uyarısı ve "✓ Sıra doğru" · künye seçilince model çözülür · "Modeli Kur" 4 kasnağı tablo sırasıyla kurar; **bırakma**: kartın üstüne bırakılan dosya okunur, ölçüm kaplaması/sihirbazı açılmaz ve yanıltıcı "okunamaz" uyarısı YOK (`data-ve-dropzone` silinince düşüyor) |
| `tests/unit/fead-checks.test.js` | `js/fead-checks.js` | **Defterden gelen üç kapı**, referansı MFSim'in kendi hesabı DEĞİL BMC defterinin sayıları: altı merkez mesafesi ve alt/üst sınırları defterle 1e-6 içinde örtüşüyor; çapın `2·rPitch` olması **on iki çapla tek tek** çıpalı — KIRPI II'de kaburgalı ve sırt kasnaklar sırayla geldiği için h_b ile h_r toplamda birbirini götürüyor ve yanlış yarıçapla kurulmuş bir kapı sınırları DEĞİŞTİRMEDEN geçerdi; en kritik çift ve %1,8'lik payı; ihlalin `'warn'` olması (kural iki kasnaklı V-kayış tahrikleri için yazılmış); **defterin kendi içindeki üç oran** (2,4492 pitch · 2,5039 dış çap · 3,13 elle) ve elle yazılanın alternatör çevresel kuvvetini %27,8 düşük göstermesi; hükmün YÖNÜ (küçültülmeli/büyütülmeli) ve ters çevrilince kırılması; devir sınırında üç noktanın doğru sınıra bağlanması (çevrim tepesi ve governed → sürekli, overspeed → anlık); **süre payı sıfır olan devrin çevrim tepesi sayılmaması** (bir gerilme noktası, çalışma noktası değil) ama payı YAZILMAMIŞ satırın sayılması; eksik veride uydurmama (`'wait'`, uygun sayılmaz); panel ve raporun AYNI çağrıyı paylaşması ve raporun `R.checks`'i yeniden HESAPLAMAMASI |
| `tests/unit/fead-pencere-ailesi.test.js` | `js/cp-core.js` dağıtımı + bütün FEAD panel kurucuları | **Her FEAD penceresi Krank Kasnağı kabuğunda** (≥2 kategori sekmesi · sağ sütun); **tepede özet şeridi YOK** (pencerenin ilk çocuğu gövdenin kendisi — kullanıcı kararı 2026-09-23) ve **"Kayış Yolundaki Yeri" YALNIZ kasnak penceresinde** (beklenen tipten: `isFeadPulley`; eski kodda 17/17 şerit, kasnaksız 7 pencerede küçük resim) — tipler `componentDefs`ten, kurucu `cp-core.js`'in dağıtımından OKUNUR (liste değil kural); pencerenin eylemi (`.ve-fp-solve`) tek ve sağ sütunda. Pencereler gerçek modelle (AG00976) çizilir; modül kartı modelini alt topolojiden kurar |
| `tests/unit/fead-wizard-catalog.test.js` | `js/cp-fead-wizard.js` + `js/fead-engines.js` + `js/fead-accessories.js` + `js/fead-checks.js` | **Sihirbazda BMC katalogları ve üç kapı.** Asıl kilitlediği şey bir TAŞIMA: durum → `veFeadWizNodes` → kurulan düğüm. Taşınmayan bir alan modülün belgelenmiş sessiz sınıfını üretir — kullanıcı sihirbazda künyeyi seçer, 7. adımda kapılar hüküm verir, kurar, kanvasta AYNI kapılar "değerlendirilemedi" der; ne hata ne uyarı çıkar. Altı alan çıpalı (`accLib` + üç devir sınırı · `engineLib` + dört motor devri) ve **sihirbazın hükmü ile kurulan modelin hükmü birebir** karşılaştırılıyor (üç durum + aksesuar devirleri + altı merkez mesafesi 6 hane). Sihirbaz kendi uygulama mantığını YAZMIYOR: `veFeadEngineApply` ve `veFeadAccApply` çağrılıyor, yani "katalogda null olan alana dokunma" ve "boş eğri yazma" kuralları tek yerde duruyor. Ayrıca: boş seçim yalnız bağı çözer, bilinmeyen anahtar hiçbir şey yazmaz, sapma adıyla yazılır, künye/motor verilmeden kapılar `'wait'` (uygun SAYILMAZ), sınırı olmayan aksesuar kartta uyarılır, çözülemeyen modelde kart `undefined`/`NaN` basmaz, kapılar YENİDEN HESAPLANMAZ, `.ve-fw-warn` ve `.ve-fw-gate-h` CSS'te tanımlı |
| `tests/e2e/fead-wizard.spec.js` | Sihirbaz (uçtan uca) | **GERÇEK tarayıcı**: çift tıkla açılış, yedi adımlık ray, örnekten doldurunca canlı şeridin `✓ model çözülüyor` + L/T/yön basması ve hata rozetinin kalmaması, **gerçek klavye girişinde odağın alanda KALMASI** (canlı şerit yama; tam yeniden çizim odağı düşürürdü), özet adımında kayış yolu şemasının SVG olarak çizilmesi, "Modeli Kur"un kanvasa 6 kasnak + 6 tel kurup **önizlemeyle birebir aynı çözümü** vermesi, modalın ekrana sığması ve gövdenin yatay kaydırmaması; **gergi satırı** 2. adımda altıncı sırada — künye seçicisi ve amber X/Y çipi yerinde, sürücü radyosu ile silme düğmesi `disabled`, koordinat alanına gerçek klavye girişi odağı düşürmeden modele işliyor ve 4. adımda aynı sayı görünüyor; **aksesuar modeli** seçilince duty hücreleri katalog sayısına dönüyor ve kaynak sütunu `katalog` yazıyor; **alt çubuk** modalın kendi başlığıyla aynı bantta (48 → 40 px, başlık 39) ve düğmeler ≤26 px; **adım rayı** üç durumu HESAPLANMIŞ RENKLE yakıyor (sınıf adı değil — `st-ok` CSS kuralını silen mutasyon yalnız buradan görünür): boş sihirbazda kırmızı + sayı, örnekte yedi yeşil + ✓, bir çap silinince o adım ayrışıyor ve kalan altısı yeşil kalıyor. Bu halkalar Node'da HİÇ koşmuyor |
| `tests/e2e/fead-wizard-tur2.spec.js` | Sihirbaz — ikinci kullanıcı turu | **GERÇEK tarayıcı**: gergi satırının TİP sütunu `Otomatik Gergi` yazıyor ve künye seçicisi onun altında, sürücü radyosu ile silme düğmesi `disabled`, **başlık↔gövde sütun hizası ≤1 px** (kullanıcının bildirdiği "garip yapı" bir CSS `display` kaçağıydı ve hizayı ancak tarayıcı ölçer); dönüş yönü düğmesine GERÇEK tık sırayı çeviriyor, `spin` +1 → −1 oluyor ve kayış boyu **13 basamak birebir** kalıyor; künye seçilince 6 alan `readonly` ama montaj konumu DEĞİL, "elle gir" seçilince kilit kalkıyor ve **künyenin değeri korunuyor** (`armLen` 56); kayış adımında Künye/Malzeme/kip seçicisi ekranda YOK, "Gereken boy" ve "KAPALI" VAR; aksesuar modeli seçilince duty okuması `2.70 → 1.74 kW` değişiyor. Bu halkalar Node'da HİÇ koşmuyor |
| `tests/e2e/fead-rozet.spec.js` | Kip anahtarı + Dönüş Yönü rozeti (gerçek tarayıcı) | **`fead-canvas-drag.spec.js`'in devamı** — ad değil ÖLÇÜLEN ŞEY devredildi. Eski dosya "kanvas = kayış düzlemi" zincirini (fare sürüklemesi → `veFeadSyncDrag` → mm → kart) ölçüyordu; o zincirin iki halkası 2026-09-09'da kalktı (kasnakların kutusu yok, kanvas↔mm köprüsü ve `fead-coordlink` gitti) ve dosya AÇILIŞTA duruyordu: `bootApp` olmayan bir adı bekleyip 60 sn sonra düşüyor, **on iki testin on ikisi de koşmuyordu**. Kalan iki halka canlı ve başka hiçbir e2e onlara bakmıyor: **kayış boyu kipi anahtarı KİLİTLİ** — 2026-09-26'dan beri Pafta'nın başlığında, kayışın kutusu yok (gergi avara merkezinden çözülünce boy bir ÇIKTI; anahtar amber ve `aria-disabled`, gerçek tık reddediliyor, metin SERBEST kalıyor, `lengthMode` hiç yazılmıyor — tıklanabilir kalsaydı anahtar "SABİT" görünür, çözücü serbest koşardı) ve **Dönüş Yönü rozeti** (tıklanınca `beltIndex` sırası "krank sabit, kalanı ters" kuralıyla çevriliyor — eski kapı bunu KABLOLARDAN ölçüyordu, kablo kalmadı; kartın `data-fead-anim` künyesindeki `spin`/`sense` dönüyor ama `loop` ve `L_eff` KIL PAYI oynamıyor, yani geometri yönden bağımsız, ikinci tık başa döndürüyor) |
| `tests/e2e/fead-checks.spec.js` | Kataloglar + üç kapı (uçtan uca) | **GERÇEK tarayıcı**: motor kataloğunun açılır listesinden GERÇEK `selectOption` ile künye yazılması (silindir · dört devir · birinci kademe çapları), aksesuar künyesinin kasnak panelinde seçilip devir sınırlarının ve 16 noktalı eğrinin dolması, Uygunluk Kapıları kartının ÇÖZÜMDEN ÖNCE üç tabloyla çizilmesi (`data-ve-fead-checks` tutamağı), kapıların BMC örneğinde gerçekten ısırması (merkez mesafesi `warn` — alternatör Ø57 olduğu için üst sınır 273,2 mm ve açıklık 281,1; devir sınırı `no` — çevrimin 2750 d/dk tepesinde alternatör 9250 > 8000 sürekli), çözümün kapıları `veFeadResults.checks`e yazması ve panelle BİREBİR aynı olması, raporun uygunluk tablosunda üç satırın da payıyla birlikte basılması, sıfır konsol hatası · **sihirbaz turu**: 6. adımda motor kataloğundan gerçek `selectOption` (silindir · dört devir · birinci kademe çapları) ve iki aksesuar künyesi (seçici SATIR SIRASINDAN değil taşıdığı SEÇENEKLERDEN bulunuyor), dört devir alanının DOM'da 700/2100/2330/2900 okunması, 7. adımda kapı kartının üç tabloyla çizilmesi ve `data-ve-fw-checks-durum` özniteliğinin `warn/ok/no` olması, "Modeli Kur"un altı yeni alanı kanvasa taşıması ve kurulan modelin kapı hükmünün sihirbazınkiyle BİREBİR aynı çıkması |
| `tests/unit/fead-sonuclar.test.js` | `js/fead-signals.js` + `js/fead-brief.js` + `js/cp-fead-results.js` + `js/cp-fead.js` sonuç durumu | **Sonuçlar sekmesinin veri katmanı + üç kusur**: "Son hesap" çözümden sonra "güncel" (eski: hep "yok"), model değişince BAYAT (imza; görünüm düğümü bayatlatmaz), kayış verisi kapalıyken senaryo/çırpma/pano frekans ÜRETMEZ (eski: 11/11 örnekte "⚠ REZONANS"); kanallar çözümün sayılarıyla BİREBİR, tarama raporun ızgarası, özetin kayma hükmü `_frSlipStats` ile aynı, kimlik addan; özet kartlarının durumu, hazır diyagramda olmayan kanal yok, senaryo notları şeride konusuna göre; **yorum TEKRAR ETMEZ** — 22 örnek×kayış durumunda "Ayır" ve ön ayar düzenlerinde hiçbir paragraf iki şeritte yazılmaz (eski: 3 527 fazla kopya) ve kümenin genel cümlesi "Ayır" ile KAYBOLMAZ (sahibi gerginin açıklığı · `sfmin` · `tc` · `tmod.1`), şerit boş kalmaz, tek kasnaklı kayma şeridi KENDİ sayısını yazar (eski: 54 şeridin 32'si zarfın değerini o kasnağın adıyla) |
| `tests/unit/fead-sonuclar-sekme.test.js` | `js/results.js` `veResultSources` + `js/graphics.js` + `js/trace-view.js` | **FEAD'in panoya kablolanması**: sekme FEAD'in kendi çözümüyle, kimlik TAM anahtarla (`~fead-crank` yakalanmaz), tek X ekseni kuralı, X seçicide yalnız küme eksenleri, **"Sonuçları Temizle" FEAD'i de siler** (eski: silmiyordu), uzlaştırma kaynak başına (Takoz FEAD'e dokunmaz), ağaç · boş durum · hazır diyagram · yorum şeridi · özet penceresi |
| `tests/e2e/fead-sonuclar.spec.js` | FEAD Sonuçlar sekmesi (gerçek tarayıcı) | **Node'da koşmayan halkalar**: "Sonuçlar'da aç" sayfayı ve sekmeyi değiştirir, müfettişi kapatır; boş pano SAYFA BOYU (hiç çizilmemiş eksen tuvali 580 px yutuyordu, alan 332 → 882 px), `:has()` kartı üstten akıtır; hazır diyagram gerçekten çizer ve yorum şeridi görünür; özet penceresi açılır, yatay taşmaz, kapanır |
| `tests/e2e/can-cozumleyici.spec.js` | `MFSim_CAN_Cozumleyici.html` | **Üretilen tek dosya**, `file://` üzerinden: açılış, sıfır ağ isteği, örnek yükleme (öksüz kimlik YOK), canvas gerçekten boyanıyor, ağaç onay kutusu, üç sekme, tema |
| `tests/e2e/measure-merge-drop.spec.js` | `js/measure-dropzone.js` + `js/trace-view.js` | MFSim'de sürükle-bırak ve çok eksenli birleştirme — araç performans VE takoz sekmesi |
| `tests/unit/komuta.test.js` | `js/cp-komuta.js` + `index.html` + `js/ribbon.js` + `build.js` | **Sipariş fişi bir SÖZLEŞME**: gidiş-dönüş kapısı (`ayristir(uret(x)) === x`) — üreteç ile okuyucu ayrı ayrı doğru olup birlikte yanlış olabilir; nottaki satır sonu ÜRETİMDE temizleniyor (geçseydi sonraki satır alan adı sanılır, `kaldir: 99` bir SİLME EMRİNE dönüşürdü); başlığı tutmayan metin `null` döner, yanlış nesne DEĞİL; alan adları diakritiksiz; değeri olmayan alan atlanmaz `(yok)` yazar. **Fiil sözlüğü** (`istek`: kaldir/duzelt/incele) bir ALAN değil bir SEÇİM — v1'de `kaldir`+`ekle` diye iki alan vardı ve `ekle` HİÇ dolmadı, dolamazdı (karşılamaya kare eklemek bir görsel yüklemek demek); her fiil gidiş-dönüşte korunuyor, id'ler diakritiksiz, **v1 fişi hâlâ okunuyor** (`kaldir:` → `istek`+`kayit`, ölü `ekle` taşınmıyor). **Ölçüm özeti** (`olcum`) belirlenimci, SIRA değişimine duyarsız (`kaldir` küme anlamlı — yeniden düzenleme sapma değil), tek kayıt değişince değişiyor ve birleştirme kaçağı yok (`["ab","c"]` ≠ `["a","bc"]`, ayırıcısız aynı özeti verirlerdi). Her tezgâh ölçtüğü kaynağı `disaAktarim` ile beyan ediyor ve o ad gerçekten dışa aktarılıyor — doğrulayıcı oradan çalışıyor. **Künye satırı boş bırakılmıyor** — okunamayınca "doğrulanmalı" yazar, yoksa "künye yok" ile "künye güncel" ayırt edilemezdi ve fişin tek sapma kemeri sessizce çözülürdü. **Tezgâhın beyan ettiği dosya diskte var** (yeniden adlandırma fişi sessizce yanlış adrese yollardı), `olc` bir FONKSİYON (pencere ölçer, beyan etmez), ölçüm gerçek `VE_KARSILAMA_GORSELLER` ile birebir. Pencere yalnız OKUR (`saveState`/mutasyon çağrısı yok), görünüm CSS'te (satır içi renk yazılmıyor), `aria-pressed` sınıfla BİRLİKTE yazılıyor |
| `tests/unit/komuta-dogrula.test.js` | `tools/komuta-dogrula.js` | **Fişin uygulanmadan önce geçtiği tek kapı — ÇIKIŞ KODUYLA ölçülüyor**, metinle değil ("uyarıyı yazdırıp 0 dönmek" kapıyı yazılı ama etkisiz bırakırdı). **Hedefsiz kaldır/düzelt DURUYOR** ("neye uygulanacak?" — sessizce geçmek kullanıcının işaretlemeyi unuttuğunu gizlerdi) ama hedefsiz İNCELE geçiyor ("şu tezgâha genel olarak bak" geçerli bir istek); v1 fişi de geçiyor. Isırdığı ölçülen altı hâl: bayat fiş (özet tutmuyor), olmayan anahtarı hedefleyen fiş (özet TUTSA bile), ölçümsüz fiş, fiş olmayan metin, bilinmeyen tezgâh, dosyası uyuşmayan fiş. **Ve ısırMAdığı hâl**: künye eski ama özet tutuyorsa çıkış kodu 0 — yanlış alarm kapıyı öldürür, ölçüldü (`main` altı PR ilerledi, karşılama listesi hiç değişmedi). Betik ikinci bir kaynak listesi tutmuyor: tezgâhın `dosya` + `disaAktarim` beyanından require ediyor (kaynak kapısı bunu tezgâh adının betikte GEÇMEMESİYLE ölçüyor) |
| `tests/e2e/komuta.spec.js` | `js/cp-komuta.js` (gerçek tarayıcı) | **Tarayıcı ölçümü = Node ölçümü** (mutasyonla doğrulandı): tezgâh kaynağını iki yerden beyan ediyor — tarayıcı için `kaynak()`, doğrulayıcı için `dosya`+`disaAktarim` — ve ayrışma SESSİZ. Ölçüldü: `takoz-ornek` `MOUNT_EXAMPLES`i çıplak adla okuyordu, o ad tarayıcıda global DEĞİL (mount-core.js bir IIFE, değer `veMountCore`ta) ama Node'da `module.exports` düz veriyor → Node 3 ölçerken pencere kendinden emin "0 kayıt" gösteriyordu. Kapı ayrıca hiçbir tezgâhın boş olmamasını da tutuyor ("veri yok" ile "kaynak yanlış" aynı görünür). Sekme gezinme: her tezgâh açılıyor, fiş AKTİF tezgâhı adres gösteriyor, **seçim tezgâha ait** (sekme değişince işaret sızmıyor, geri dönünce duruyor), liste düzeni tek sütun ve uzun dosya adları taşırmıyor. Ayrıca **üç halka Node'da HİÇ koşmuyor**: (1) gömülü künye + gömülü kare yalnız build çıktısında var — 28 başlığın dolu olması ve küçük resimlerin GERÇEKTEN çözülmesi (`naturalWidth > 0`) ancak burada ölçülür; (2) jsdom `:hover`ı da `:focus-visible`ı da hesaplamaz — fare zemini değiştiriyor, seçili kartın çerçevesi ayrışıyor ve karesi soluyor; (3) tıklama → seçim → fiş zinciri, ikinci tıkla geri alma dahil. Ayrıca: şifresiz tezgâh görünmüyor ve hatalı şifre söyleniyor, ikinci açılışta şifre SORULMUYOR (sessionStorage) ve işaret duruyor, ESC kapatıyor, ızgara yatay kaydırmasız sığıyor. **`aria-pressed` hatası burada yakalandı** — birim testi geçiyordu |
| `tests/e2e/mufettis-sigma.spec.js` | Müfettiş sütunu (gerçek tarayıcı) | **Sütuna giren pencere okunur, sığmayan modal**: her modülün örneği yüklenir, paletteki her tipin penceresi açılır; taşma yok ve dört okunurluk ölçüsü (düzenlenen sütun ≥ %90, girdi ≥ 56 px, etiket kırpılmıyor — çizilmiş metin kutusuyla, başlık büzülmüyor, İÇERİDE YATAY KAYDIRMA YOK) her kategori sekmesinde; FEAD modül kartı kökte aynı işlevle ölçülür; `VE_SUTUNA_SIGMAYAN`daki tip modal açılır; **düğme yazısı iki satıra kırılmıyor** — sütunda da modalda da, çizilmiş metin kutularından (motor + üç aksesuar penceresinde kırılıyordu) |
| `tests/unit/axis-format.test.js` | `js/graphics.js` + panel grafikleri | Eksen basamağı adımdan; MFSim ↔ Görüntüleyici biçimleyicilerinin uzlaşması; **panel eksenleri yuvarlak adımla** (`veEksenBolme`: ölçülen eski eksenler 1265 · 949 · 633 yuvarlak bölmeye oturuyor, aralık genişlemez, yakınlaştırmada da yuvarlak, kayan nokta artığı etikete sızmaz, 500 rastgele aralıkta adım 1·2·5 × 10ⁿ) + kaynak kapısı (Motor · Tork Konvertörü · yol profili yardımcıdan geçer, eski dörde bölme döngüsü yok, Tork Konvertörü tuvale beyaz çizmez) |
| `tests/unit/solver-uyari-ozeti.test.js` | `js/solver-pro.js` | **Uyarı özeti saymaz**: 8 uyarı → 8 satır ve "(8 uyarı)" (eskiden 18), her satırda tek "⚠", girdi listesi değişmez; özet sayılmayan `warn-ozet` tipiyle yazılıyor |
| `tests/unit/ayarlar-kimlik.test.js` | `js/settings.js` + `css/styles.css` | Ayarlar'ın yazdığı her `ve-set-*` sınıfının CSS kuralı var (üçü de yoktu); avatar hesap menüsünün dairesi (tek kaynak, boş hâli dâhil); not kardeş açıklamanın ölçüsünde; Görünüm sayfasında tek büyük başlık, Kimlik ve Tema ara başlık |
| `tests/unit/panel-dugme.test.js` | `css/styles.css` `.sw-btn*` | Düğme yazısı kırılmaz, satır kayar — hızlı kapı; asıl ölçüm `mufettis-sigma.spec.js` |
| `tests/unit/denetim-dili.test.js` | `css/styles.css` | **Denetim dili** (11·B · 12·B): standart `scrollbar-*` yalnız Firefox bloğunda, `::-webkit-scrollbar` 8 px yuvarlak; liste okunu TEK kural çizer (çizgi ok: dört duraklı iki gradyan, `--text-muted`, `currentColor`/`url` yok); sayı alanı oksuz |
| `tests/e2e/minimap-ortmez.spec.js` | `js/minimap.js` (gerçek tarayıcı) | 1366×657'de FEAD (iki örnek × panel kapalı/açık), AP ve Takoz örneklerinde minimapın örttüğü GÖRÜNEN denetim 0 (kaydırma kabının dışına taşan satır sayılmaz) ve panel açılınca kutu iniyor; 1920×945'te köşe boşsa kutu AÇIK kalıyor; düğme oto inmiş kutuyu açıyor, istek köşe boşalınca sönüyor, kullanıcının indirdiği kutu inik kalıyor ve tercih yazılıyor |
| `tests/e2e/pencere-ailesi.spec.js` | pencere başlıkları (gerçek tarayıcı) | Dokuz pencere (Özellikler · Ayarlar · Program Durumu · İçe Aktarma · Kısayollar · Kılavuzlar · Program Arşivi · Tablo penceresi · Çözücü): bant = `--bant-h` ve zemini bandın, 1 px çizgi, başlık yazısı "Bileşenler" başlığınınki, kapat 22×22 ve içinde maskeli çizgi ikon, başlıkta ikon, köşe `--radius-lg`, gölge `--shadow-xl` — sapmaları pencere adıyla sayar |
| `tests/e2e/kaydirma-cubugu.spec.js` | `css/styles.css` kaydırma çubuğu (gerçek tarayıcı) | Çubuk GERÇEKTEN çiziliyor (Playwright'ın `--hide-scrollbars` bayrağı kapalı): sentetik kutu ve Ctrl+K listesi 8 px, `scrollbar-width` auto |
| `tests/unit/veri-izgara.test.js` | `js/cp-engine.js` · `cp-torque-converter.js` · `cp-gearbox.js` · `cp-drivetrain.js` + `css/styles.css` | **Veri ızgarası tablo gibi** (13·B): satır üreteçleri girdiye satır içi zemin/çerçeve yazmıyor, silme düğmesi sade (`ve-row-del`), beş ızgara `ve-izgara` taşıyor, CSS dinlenmede şeffaf · fare/odakta kutulu, dikey çizgi yok |
| `tests/unit/rapor-hiza.test.js` | `js/results.js` `_ftBuildTable` + `css/styles.css` `.dr-body` | **Rapor sayısı sağa** (14·B): vites geçişi tablosunun sekiz sayı sütunu hücre ve başlıkla sağa, vites adı solda; `.dr-body table td` rengi ezmiyor; rapor üretecinde sabit onaltılık renk yok |
| `tests/e2e/rapor-hiza.spec.js` | Ayrıntılı rapor (gerçek çözüm, gerçek tarayıcı) | Tamamı sayı olan her sütun (≥ 30 sütun, ≥ 1000 hücre) hücresiyle ve başlığıyla sağa; satır içi rengi CSS'in ezdiği hücre 0 |
| `tests/unit/tablo-pencere.test.js` | `js/tablo-pencere.js` | **Sütuna sığmayan tablo açılır pencerede**: karar ölçümden (sığmayan katlanır + özet kartı, sığana dokunulmaz, gizli sekmede karar verilmez), pencere birimin KENDİSİNİ taşır (kopya değil — olay işleyicileri aynı modeli yazar), kapanınca kartın arkasına döner, panel yeniden çizilince yeni birimi alır, başka düğüme geçince kapanır, TEK ESC (alttaki dinleyici görmez), odaktaki hücre sökülmeden bırakılır |
| `tests/e2e/tablo-pencere.spec.js` | `js/tablo-pencere.js` + FEAD Çözücü + AP eşleştirme (gerçek tarayıcı, `test:urun`) | **Sütunda yatay kaydırma yok** (FEAD her pencere × her sekme; AP eşleştirme — eski: 785/359 ve 337/335 px); çalışma çevriminde kart → küçük pencere (< %60 ekran, kaydırmasız, müfettişin üstünde, odak içeride) → hücre modeli yazar → satır pencereden eklenir → TEK ESC → hata bildirimi yok (ResizeObserver döngüsü bir kez kullanıcıya gösterildi) |
| `tests/unit/theme-font.test.js` | `js/theme.js` `veThemeFs`/`veThemeFont` + bütün tuval çizen `js/` | **Tuvalin yazı BOYU da ölçekten**: `veThemeFont` sayıyla değil basamak adıyla çağrılır (istisna yalnız 3B etiket dokusu), her ad CSS ölçeğinde var, en küçük tuval yazısı `--fs-micro`, köprünün yedek boyları CSS'le aynı, rem kökün yazı boyuyla çarpılır; **renk de köprüden** — `fillStyle`/`strokeStyle`/`shadowColor` `color-mix(` ya da `var(--` taşımaz (tuval sessizce yok sayar, önceki rengi kullanır) |
| `tests/unit/panel-chart.test.js` | `js/panel-chart.js` | Zoom penceresi, isabet testi ve **`pcEtiketYerlestir`** — üst üste düşen grafik etiketleri alt alta dizilir, sabit engelin altına iner |
| `tests/unit/tek-yazi-tipi.test.js` | `css/styles.css` + `js/` + `css/fonts.css` | **Tek yazı tipi (Inter)**: CSS'te aile yalnız `inherit` / `var(--font-sans)` (istisna: TXT rapor `<pre>`), UA'nın `kbd/code/samp/pre` mono'su sıfırlanır, ağırlıklar gömülü setten, JS'te her `.font =` `veThemeFont()` köprüsünden, satır içi aile yok, Plotly köprüden; **5·B takma aile**: `MFSim Segoe` yalnız yerel adlı sekiz yüz, 400/500/600/700/800 doğru yüze (italikleriyle), aralıklar 100–1000 boşluksuz, belgelere yalnız gömülü yüz taşınır |
| `tests/e2e/tek-yazi-tipi.spec.js` | Ürünün kendisi (gerçek tarayıcı, `test:urun`) | Inter GERÇEKTEN yüklü (Türkçe harfler, iki ağırlık, `measureText` ile — `document.fonts.check` boştur), ekranda 500+ öğe ve TUVALDE tek aile, sıfır ağ isteği |
| `tests/e2e/karsilama-kenar.spec.js` | Karşılama ekranı (gerçek tarayıcı) | **Karşılama fotoğrafı ekranın tamamı**: çizilmiş dikdörtgenler ölçülür — foto (0,0)'dan ekran boyunca ve açılış ekranınınkiyle aynı, kart açılış amblemiyle aynı sol kenarda (x), köşe yarıçapı 0 ("çukur MODÜLDE geri geliyor" halkası 2026-09-23'te kalktı: çukur tuvalden de kalktı, tuvalin yapışıklığı `kabuk-sutun.spec.js`te) |
| `tests/e2e/marka-tutarli.spec.js` | Açılış → karşılama markası (gerçek tarayıcı) | **Marka iki ekranda AYNI aile, ağırlık ve em-iz** (boy bilerek farklı: amblemde 80 px, logoda 20 px); genişlik halkası 0,7 px'lik iz farkının göze görünür olduğunu (~3,5 px) kanıtlar; **devir teslim uçuşu ÇİZİMDE ölçülür**: ad ve çark klonları dondurulur, kalkış karesinde kaynağın, iniş karesinde logonun üstündeler (x, dikey orta, genişlik < 0,5 px) — hedef karşılamanın giriş koreografisi başladıktan SONRA ölçülürse marka 16 px solda, 14 px aşağıda iner, amblem kendi izini yazarsa inişte 3,5 px dar kalır |
| `tests/unit/kabuk-bant.test.js` | `css/styles.css` kabuk bantları + tuval | **Kabuğun üst bandı TEK jeton** (`--bant-h` + `--bant-zemin`): altı bant ("Bileşenler" · sekme bandı · müfettiş başlığı · Veri Gezgini · araç çubuğu · rapor bandı) ölçüyü ve zemini jetondan alır, KURAL olarak: jetonu kullanan her kural zemini ve 1 px alt çizgiyi de taşır (açık `ALT_CUBUKLAR` listesindeki pencere alt çubuğu çizgiyi üstte taşır — sihirbaz); eski ad (`--results-bar-h`) kaynakta yok; Topoloji bantlarında dikey pay yok; sekme bandının çizgisi zeminin iç gölgesi (aktif sekme örtebilsin); tuvalin kenarlığı/köşesi/gölgesi ve kabın payı/aralığı YOK. Eski CSS'te 7/7 düşüyor; **bant İNCE** — `--bant-h` 26 px (36 "gereksiz kalın", 30 "hâlâ boyuna geniş"), araç çubuğunun dikey payı 1 px, bant düğmeleri TEK boyda (segment = düğme = İçe Aktar = 22 px); **şerit bandı İNCE** — `--ribbon-strip-h` 32 px (38'di), ▼ düğmesi komşularından uzun değil, denetimlerin altında/üstünde ≥ 3 px, açık şeridin gövdesi aynı boyda (94 px). Eski CSS'te 3 test, altı geri alma mutasyonunun her biri en az 1 test düşürüyor |
| `tests/e2e/kabuk-sutun.spec.js` | Kabuk yerleşimi (gerçek tarayıcı) | Ray + palet tek sütun, durum şeridi tuvalin altında, şerit katlaması; **KABUK TEK ÇİZGİ** — müfettiş açıkken üç başlığın alt çizgisi ve ölçüsü aynı (eski: y=75·67·71, 37·29·33 px), yazıları aynı ortada, tuval dört kenarda komşusuna yapışık, aktif sekme bandın çizgisini örtüp tuvalin zeminini taşıyor; **başlıklar içeriğin kenarında** ("Bileşenler" simgesi = kategori başlığı = öğe simgesi, eski 76↔81; müfettiş başlığı = içerik kenarı, eski 1233↔1231) ve kimlik satırı iç kimliği değil TİPİ söylüyor ("ID: comp-4" yok); **BANT İNCE** — altı bant (Topoloji'nin üçü · Sonuçlar'ın ikisi · rapor bandı; Kayış Tablosu çekmecesinin bandı çekmeceyle emekli) tam jeton, ölçü **Segoe UI'ın satırıyla** (Inter'e metrik taklidi — kullanıcının yazısı; sekme payını 5 px'e çıkaran mutasyon Inter'le 26 = jeton ile GEÇERDİ, Segoe satırında 27 ile düşüyor); eski 30 px'te de düşüyor; palet liste satırı; **palette hiçbir ad kesilmiyor** (üç modülün içinde; ad iki satıra kadar sarar — "Motor-Konvertör Eşleştirme" 156/153 px kesiliyordu) |
